import { type Db } from "./db.js";
export interface OperationContext {
    repoId: number;
    actor: string;
    taskId?: string;
    requestId: string;
    now: string;
}
export interface OperationMeta {
    operation: string;
    repoId: number;
    requestId: string;
    at: string;
}
export type OperationResult<T = unknown> = {
    ok: true;
    data: T;
    meta: OperationMeta;
} | {
    ok: false;
    error: {
        code: string;
        message: string;
        details: unknown;
        nextSafeActions: string[];
    };
};
export declare const OPERATION_EXIT_CLASS: Record<string, number>;
export declare class OperationDispatcher {
    private readonly db;
    private readonly options;
    private readonly service;
    constructor(db: Db, options?: {
        repoRoot?: string;
    });
    operations(): string[];
    dispatch(operation: string, context: unknown, input?: unknown): OperationResult;
    private invoke;
    private gitSemanticRoot;
    private dispatchGitKnowledge;
    private syncGitFeatureIdentities;
    private dispatchRequest;
    private dispatchDistillMutation;
}
