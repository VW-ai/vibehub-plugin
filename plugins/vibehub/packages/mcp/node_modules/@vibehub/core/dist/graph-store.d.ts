/** Read-only compatibility/read-model facade after the canonical KB cutover. */
import type { TerritoryLayout, Territory } from "./contract/map-types.js";
import type { Db } from "./db.js";
export declare function listTerritories(db: Db, repoId: number): Territory[];
export declare function readTerritoryLayouts(db: Db, repoId: number): Map<string, TerritoryLayout>;
export declare function countAnchoredFiles(db: Db, repoId: number): number;
export declare function featuresForFile(db: Db, repoId: number, file: string): string[];
export interface SpecRow {
    id: string;
    repoId: number;
    featureId: string | null;
    type: "intent" | "decision" | "constraint" | "convention" | "contract" | "context" | "change";
    state: "draft" | "active" | "stale" | "superseded" | "deprecated";
    summary: string;
    detail: string | null;
}
export type SpecType = SpecRow["type"];
export interface KnowledgeResult {
    spec: SpecRow;
    matchedPaths: string[];
    score: number;
    sourceKind: "canonical";
}
/** @deprecated New adapters use KnowledgeService.searchSpecs. */
export declare function retrieveKnowledge(db: Db, repoId: number, input: {
    query?: string;
    paths?: string[];
    limit?: number;
    includeDrafts?: boolean;
    includeHistory?: boolean;
}): KnowledgeResult[];
export declare function readSpec(db: Db, repoId: number, id: string): SpecRow | null;
/** @deprecated Pass repoId explicitly. */ export declare function readSpec(db: Db, id: string): SpecRow | null;
export declare function edgesFrom(db: Db, fromId: string, repoId?: number): Array<{
    toId: string;
    type: string;
}>;
export declare function setSetting(db: Db, key: string, value: string, repoId?: number): void;
export declare function getSetting(db: Db, key: string, repoId?: number): string | null;
