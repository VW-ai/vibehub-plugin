import type { ConflictCardSnapshot } from "./contract/conflict-types.js";
import type { TaskPanelSnapshot } from "./contract/panel-types.js";
import type { WorkbenchReadWarning } from "./contract/workbench-bridge.js";
import type { Db } from "./db.js";
export interface ReadModel<T> {
    data: T;
    warnings: WorkbenchReadWarning[];
}
export declare function readTaskPanelModel(db: Db, repoId: number, repoRoot: string, taskId: string, capturedAt: string): ReadModel<TaskPanelSnapshot> | null;
export type ConflictDetailModel = {
    status: "ok";
    data: ConflictCardSnapshot;
} | {
    status: "not_found";
} | {
    status: "evidence_unavailable";
    message: string;
};
export declare function readConflictDetailModel(db: Db, repoId: number, repoRoot: string, conflictId: string, capturedAt: string): ConflictDetailModel;
