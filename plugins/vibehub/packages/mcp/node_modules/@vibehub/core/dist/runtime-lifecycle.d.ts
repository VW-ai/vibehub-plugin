export type ManagedAssetRepairPolicy = "replace-managed" | "verify-only";
export interface ManagedAssetDefinition {
    /** Stable provenance label, normally builtin://... inside the release. */
    source: string;
    /** Absolute installed target owned by VibeHub. */
    target: string;
    content: string;
    checksum: string;
    version: string;
    repairPolicy: ManagedAssetRepairPolicy;
}
export interface ManagedAssetManifest {
    schemaVersion: 1;
    releaseVersion: string;
    assets: ManagedAssetDefinition[];
}
export type ManagedAssetStatus = "healthy" | "installed" | "repaired" | "conflict" | "missing";
export interface ManagedAssetResult {
    source: string;
    target: string;
    checksum: string;
    version: string;
    repairPolicy: ManagedAssetRepairPolicy;
    status: ManagedAssetStatus;
    actualChecksum?: string;
    message?: string;
}
export interface InitRuntimeOptions {
    repoPath: string;
    dbPath?: string;
    stateDir: string;
    /** Canonical containment root for every managed target. */
    allowedAssetRoot: string;
    manifest: ManagedAssetManifest;
    now?: () => Date;
    /** Deterministic test seam for proving multi-file rollback. */
    managedAssetFault?: (event: {
        phase: "before-target-rename" | "after-target-rename" | "before-ledger-rename" | "after-ledger-rename";
        committedTargets: number;
        target: string;
    }) => void;
}
export interface InitRuntimeResult {
    ok: boolean;
    dbPath: string;
    schemaVersion: number;
    repo: {
        id: number;
        root: string;
        slug: string | null;
        defaultBranch: string;
    };
    managedAssets: ManagedAssetResult[];
    conflicts: ManagedAssetResult[];
}
export interface DoctorRuntimeResult {
    schemaVersion: 1;
    healthy: boolean;
    db: {
        status: "healthy" | "missing" | "unreadable" | "migration_required";
        path: string;
        schemaVersion: number | null;
        expectedSchemaVersion: number;
        sqliteVersion: string | null;
        message?: string;
    };
    nativeDependency: {
        status: "healthy" | "unavailable";
        module: "better-sqlite3";
        message?: string;
    };
    repo: {
        status: "healthy" | "uninitialized" | "invalid";
        root: string | null;
        id: number | null;
        message?: string;
    };
    managedAssets: {
        status: "healthy" | "unhealthy";
        releaseVersion: string;
        assets: ManagedAssetResult[];
    };
}
export declare const sha256: (content: string | Buffer) => string;
export declare function initializeRuntime(options: InitRuntimeOptions): InitRuntimeResult;
export declare function doctorRuntime(options: InitRuntimeOptions): DoctorRuntimeResult;
