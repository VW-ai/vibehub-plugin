import type { Db } from "./db.js";
export interface ScopePattern {
    mode: "write" | "read";
    glob: string;
    label: string | null;
}
export declare function canonicalRepoPath(input: string): string;
export declare function matchesScopePattern(glob: string, repoPath: string): boolean;
export declare function replaceScopePatterns(db: Db, repoId: number, taskId: string, status: string, patterns: Array<{
    mode: "write" | "read";
    glob: string;
    label?: string;
}>): void;
export declare function readScopePatterns(db: Db, taskId: string): ScopePattern[];
/**
 * Atomically claim the declaration's one off-scope reminder. Read patterns
 * never authorize writes; no declaration means there is no claim to compare.
 */
export declare function claimOffScopeReminder(db: Db, taskId: string, repoPath: string, now: string): boolean;
