import type { AppliedIntervention, ConflictCardSnapshot, MapSnapshot, TaskPanelSnapshot, WorkbenchIntervention, WorkbenchBridgeResult, WorkbenchRepoRef } from "./contract/index.js";
import { type Db } from "./db.js";
import { type DoctorRuntimeResult, type InitRuntimeResult, type ManagedAssetManifest } from "./runtime-lifecycle.js";
import { type ProjectActivationOptions, type ProjectActivationResultV1 } from "./project-activation.js";
export interface RuntimeServiceOptions {
    dbPath?: string;
    openDatabase?: (path: string) => Db;
    now?: () => Date;
}
/** Canonicalize an explicitly supplied path; there is deliberately no default. */
export declare function resolveWorkbenchRepoRef(explicitRepoPath: string, repoKey?: string): WorkbenchRepoRef;
/**
 * Core-owned read boundary used by native and development hosts. It never
 * creates a database on a read: a missing SQLite file is a setup state, not
 * an invitation to manufacture an empty successful workbench.
 */
export declare class RuntimeService {
    #private;
    constructor(options?: RuntimeServiceOptions);
    initialize(repoPath: string, stateDir: string, allowedAssetRoot: string, manifest: ManagedAssetManifest): InitRuntimeResult;
    doctor(repoPath: string, stateDir: string, allowedAssetRoot: string, manifest: ManagedAssetManifest): DoctorRuntimeResult;
    inspectProjectActivation(repoPath: string, stateDir: string, allowedAssetRoot: string, manifest: ManagedAssetManifest): ProjectActivationResultV1;
    applyProjectActivation(repoPath: string, stateDir: string, allowedAssetRoot: string, manifest: ManagedAssetManifest, overrides?: Pick<ProjectActivationOptions, "instructionVersion" | "instructionBody" | "instructionFault">): ProjectActivationResultV1;
    readProjectActivationStatus(repoPath: string, stateDir: string, allowedAssetRoot: string, manifest: ManagedAssetManifest): ProjectActivationResultV1;
    readWorkbenchSnapshot(repo: WorkbenchRepoRef): WorkbenchBridgeResult<MapSnapshot>;
    readTaskPanel(repo: WorkbenchRepoRef, taskId: string): WorkbenchBridgeResult<TaskPanelSnapshot>;
    readConflictDetail(repo: WorkbenchRepoRef, conflictId: string): WorkbenchBridgeResult<ConflictCardSnapshot>;
    applyIntervention(repo: WorkbenchRepoRef, requestId: string, intervention: WorkbenchIntervention): WorkbenchBridgeResult<AppliedIntervention>;
}
