import type { Db } from "./db.js";
export interface CanonicalConflictPair {
    taskIds: [string, string];
    sides: [string, string];
}
export declare function canonicalConflictPair(db: Db, taskIds: [string, string]): CanonicalConflictPair;
export declare function isConflictPairIgnored(db: Db, repoId: number, taskIds: [string, string]): boolean;
export declare function persistIgnoredConflictPair(db: Db, repoId: number, taskIds: [string, string], now: string): CanonicalConflictPair;
/** Resolve either a rich local id or a merge-tree summary id to one pair. */
export declare function resolveConflictPair(db: Db, repoId: number, conflictId: string): CanonicalConflictPair | null;
