# Ticket-first runtime dogfood

This log records the bounded use of VibeHub to develop its own Ticket-first
runtime alignment. It is acceptance evidence for `ticket-first-runtime-pivot`,
not the historical M5 parallel-path and recovery proof.

## Plan and validation observations

| Stage | Observation | Evidence | Disposition |
| --- | --- | --- | --- |
| Context query | Sandboxed governed KB reads failed as `validation_error: unable to open database file`, without the database path or a permission-oriented recovery hint. The same query succeeded once access to the machine-local VibeHub state directory was granted. | `kb.status` requests `ticket-first-context-status` and `ticket-first-context-status-3` | Follow-up candidate: preserve the underlying error class/path and suggest local-state permission recovery. |
| Source runtime | A source checkout without `packages/cli/dist/main.js` falls back to the versioned plugin runtime. Because commits after tag `v0.3.0` still declare version `0.3.0`, the cache can appear current while source behavior has advanced. This did not cause the database permission failure, but it is a reproducible version-skew risk for source dogfood. | Source wrapper resolution in `skills/scripts/_dispatch.mjs`; HEAD `63043ad`; package/plugin version `0.3.0` | Follow-up candidate: make source-tree wrappers fail clearly or prefer an explicit source-build marker when the checkout is newer than the cached release. |
| Existing graph | Historical implementation Tickets remain mostly `BLOCKED` although their code is already merged, because the migrated graph has no corresponding Git-native Outcomes. | Snapshot `tgs-74ea3c3a514fa83d4939cee3dfab4e9e3276c77e6406299526511171383556cb` | Treat as migration/adoption debt; do not rewrite the historical graph inside this bounded pivot. |
| Candidate authoring | Exact patch authoring requires extracting a small operation schema from a 28k-line generated contract and manually carrying five source-binding fields. | `skills/contracts/operation-contracts.json`; mechanical validation of `.ticket-first-runtime-pivot.candidate.json` | Follow-up candidate: provide a deterministic candidate scaffold command from a snapshot. |
| Context compilation | `ticket.context.compile` acquires a Git-admin writer lock and writes a ContextBinding, although the operation name reads like a pure compiler. In a sandbox it first failed with a generic lock error; after permission was granted it succeeded and changed `semanticLedgerDigest`, invalidating an in-flight plan candidate with unchanged topology. | Binding `tcb-848e0e7f6a3b7261ff8e6c4eb98265840ab63f6b07a9d08e03fc9151237c5a8b`; source tokens `tls-fe91…` then `tls-c1bc…` | Keep fail-closed CAS. Follow-up candidate: make the write effect and required Git-admin permission explicit in naming/help/receipts. |
| Independent validation | The first candidate attempted to broaden Activated from KB value to Ticket frontier value. Validation rejected that as an ungoverned product-contract change. | Independent validation of the first candidate; active `decision-workbench-015` | Fixed in plan: activation semantics are unchanged in this Ticket. |
| Context quality | A later candidate could suppress Ticket-mode reminders while active `intent-workbench-003` still promised periodic injection. Validation rejected the resulting code/knowledge drift. | Independent validation against governed `intent-workbench-003` | Fixed in plan: canonical dual-mode checkpoint knowledge and its receipt are now acceptance evidence. |
| Governed read concurrency | An independent validator's `kb.status`, `kb.spec.get`, and `kb.spec.search` reads failed with `internal_error: finalized mapping is immutable`. A later single `kb.status` retry succeeded without repository or database repair, pointing to a concurrent semantic-store materialization race rather than corrupt canonical data. | Validator run after snapshot `tgs-af7a…`; successful retry `reproduce-finalized-immutable-status` | Work around by serializing governed reads for this Run. Follow-up candidate: make canonical-store read materialization idempotent under concurrent processes and test the race. |

## Run and evidence observations

| Stage | Observation | Evidence | Disposition |
| --- | --- | --- | --- |
| Plan apply | The independently validated candidate applied exactly and created one READY Ticket without changing unrelated topology. The returned `after` source was immediately usable by the frontier. | `ticket.worktree.patch` request `ticket-first-plan-apply-20260731-01`; Ticket revision `sha256:8fa2f5…` | Healthy; keep exact-source apply and independent validation. |
| Context compile | Compiling the pivot returned the exact ten referenced files and a binding, but the single-line JSON response was about 24k tokens and was truncated by the host display. The critical binding/source fields were present near the start, while the repeated full packet made routine consumption harder. | ContextBinding `tcb-6d079c…`; request `ticket-first-run-compile-20260731-01` | Follow-up candidate: add a compact receipt/projection or wrapper output mode without weakening the full canonical envelope. |
| Run claim | The newly created independent Ticket appeared READY and was claimable from its exact ContextBinding with no prerequisite or hidden conversation dependency. | Run `trn-4ac944…`, generation 1; frontier source `tls-dccce…` | Healthy; the Ticket carried sufficient execution authority after the governed context checks. |
| Dual-mode implementation | The initial prototype incorrectly made Ticket-first SessionStart universal. Executing from the Ticket acceptance made the missing context-only branch explicit, leading to deterministic routing by the canonical ledger marker and tests for both modes. | `packages/core/src/hook-ingest.ts`; focused Core run with 48 passing tests | Fixed in Run: Ticket-managed receives Ticket-first guidance and shadow cadence; context-only retains context-first guidance and reminder cadence. |
| Context truth | The runtime change would have drifted from active canonical intent if code were changed alone. A governed amend created active revision 2 with the user's Context-quality requirement, both implementation hashes, and unchanged quality boundaries. | `kb.amend` request `ticket-first-ingest-amend-20260731-01`; `intent-workbench-003` revision 2 | Fixed in Run through the canonical operation; no direct META or semantic-store hand edit. |
| Skill boundary | The first setup-guidance edit named `.vibehub/tickets/protocol.yaml` directly. The skill-package validator rejected it because semantic Skills must not infer authority from persistence files or formats. | Initial failing `packages/cli/test/skill-package.test.ts`; corrected run 37/37 | Fixed in Run: Core performs the mechanical detection; setup consumes governed runtime capability and stays behind the operation boundary. |
| Test environment | The first full CLI run failed ten review-host cases because the workspace sandbox denied loopback listen with `EPERM`. The exact suite passed 122/122 when explicitly allowed to bind `127.0.0.1`. | Sandboxed `pnpm test`; escalated `pnpm --filter @vw-ai/vibehub-cli test` | Environment limitation, not a product defect. Preserve the explicit loopback security tests. |
| Verification | Build and typecheck passed; Core passed 454/454, CLI passed 122/122, MCP passed 25/25, and bounded local dogfood passed while asserting both SessionStart modes. | `pnpm build`, `pnpm typecheck`, package test runs, `pnpm dogfood` | Acceptance evidence; full historical M5/release-host dogfood intentionally remains out of scope. |

Independent closeout is recorded in the Ticket semantic ledger and is
intentionally not a precondition for this file.
