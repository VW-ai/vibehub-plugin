progress:
  completion: 0.78
  milestones:
    - id: m1-map-main
      name: "Map main view (scope occupancy) — dynamize from approved v8"
      status: completed
      gate: true
      notes: "S3-S5 done. S5: interaction suite 29 tests (correlate-hover exact footprints + .14 dim, reverse territory→rail correlate implemented, legend filters x4, tooltip 260ms/flip/exact-100k, +N chip, switcher, 5 states, keyboard parity, overlap checks @1280x800+1440x900) — 35/35 green incl. parity shots; legend-over-bottom-row fixed via density legend band (48px, only past v8's 92.5% max bottom); entry-animation fill:both bug fixed (was defeating correlate dim + offsetting fixed tooltip). Scale-extremes protocol closed with per-component evidence table in notes/map-main.md. 3 new forks logged to DECISIONS-NEEDED."
    - id: m2-task-panel
      name: "Task panel (timeline + intervention)"
      status: completed
      gate: true
      notes: "S1-S5 done. S5: interaction/state suite 19 tests in tests/panel-interactions.spec.ts — open paths (card click/Enter/Space, role=dialog labeled), close paths (X/Escape/scrim) ALL returning focus to the opening card (implemented this iter: App.tsx openerTaskId ref + closePanel refocus via rAF, Escape rerouted through closePanel; keyboard-parity principle), open-at-newest mechanically verified + seam shadow on/off/on with scroll, Milestones 10→3→10 (2 user-voice + 1 ask, launch first/question last) and marathon 60→14, file_change expand/collapse with off-scope files probed to EXACTLY --clash-ink computed color, tail toggle + pressed state both directions, autogrow 52→124 exact cap + internal scroll + deck pinned + returns to 52, mode placeholder narration (exact copy both modes), panel tooltips honor 260ms (nothing at 80ms) with real content (age/Terminate/seg), synthetic honesty (queued g-task-queued → 0 rows + 'Not launched yet' + empty tail; running g-task-running → exactly 1 launch row, nothing invented), ?panel=marathon (60 rows, session 12/12, scroll range>500, deck pinned, zero console errors), geometry @1280x800+1440x900 in the busiest state (burst+tail open): strict section stacking phead→tlbar→tl→tail→deck, no overlap/clipping, deck pinned to panel bottom, action buttons unclipped, Terminate gap>24px. Gates: tsc clean; pnpm build green; FULL suite 59/59 (map 35 + panel-parity 5 + new 19; one test authoring fix — v8 running cards carry conflicts so the RUNNING-pill synthetic check uses forty-territories' conflict-free g-task-running). SCALE-EXTREMES PROTOCOL closed: per-component evidence table (identity row / timeline / entries / deck / tail × N=0/1/many, TEXT, NUMBER, SPACE, DYNAMIC, SCREEN) in notes/task-panel.md, every cell citing code or a test. 2 forks to DECISIONS-NEEDED (focus-return re-activates correlate; Terminate-gap threshold 24px). — S4 (done earlier): panel dynamized into React (TaskPanel / PanelIdentity / Timeline / TimelineEntry / TranscriptTail / InterventionDeck), consuming ONLY panel fixtures — zero hardcoded content; view helpers in panel-derive.ts (timeTip/panelScopeChips/sessionMeta/twistView/deckPlaceholder). Map integration: rail card click (+ Enter/Space, keyboard parity) opens the panel 200ms over blurred(1.5px)+scrimmed(.22, whole .main incl rail) map; close = X/Escape/scrim; fixture switch closes; App owns state, remount per task via key. Wiring: task-refactor-auth → panel-refactor-auth (S2 verbatim); other cards → synthetic panel (launch + state transition ONLY, honesty rules in fixtures/synthetic-panel.ts; queued = honest empty timeline); ?panel= dev param reaches extremes (marathon 60/14 rows session 12/12 3h, just-launched 1/1 42s, quiet-milestones 8/3 2d — all verified live, zero console errors). Milestones toggle renders the DERIVED 023 tier: auth timeline 3 of 10 entries (visual diff documented in notes). All S2 behaviors preserved & verified: open-at-newest + seam shadow, file_change expand w/ off-scope amber, tail toggle w/ pressed state, autogrow 52→124 (deck pinned 776==776), mode placeholder narration, shared 260ms Tooltip. Found+fixed real port bug: S2's .ev .sub collided with the map's absolute .sub chips (renamed .subnote). Gates: tsc clean; pnpm build green; Playwright 40/40 = existing 35/35 untouched + 5 new capture tests; parity shots task-panel-s4-* @1280+1440 (+expanded, +marathon, +regenerated static ref), deltas documented in notes/task-panel.md. 3 forks to DECISIONS-NEEDED (synthetic-launch stand-ins, scrim-covers-rail, suite growth bookkeeping). — S3 (done earlier): Types: src/panel-types.ts extends src/types.ts (Task/scopes/git reused, zero duplication) — TimelineEvent 11-member discriminated union, every member annotated with its source hook (UserPromptSubmit/PostToolUse/Notification/Stop) or git/gh fact; SessionIdentity + TwistEvidence; no sentiment/confidence/progress-% fields. Milestone tier DERIVED (isMilestone in src/panel-derive.ts, 023 mechanical whitelist: commit/state_transition/launch/injection/question-as-transition-carrier), never stored. relAge gained a documented day rung (s/m/h/d). Fixtures (TS literals + satisfies): panel-refactor-auth (S2 content verbatim, 10 events, 12m), panel-just-launched (N=0 agent events, 42s), panel-marathon (60 deterministic events, session 12/12, ~700-char launch prompt, 3h), panel-quiet-milestones (2d, milestone tier = exactly 3). Gates: tsc --noEmit clean, pnpm build green; runtime invariants spot-checked (counts/ages/sort/id-uniqueness). Type→hook mapping table in notes/task-panel.md. 3 forks to DECISIONS-NEEDED (023-vs-S2 milestone split, file_read 11th member, day-rung rounding). — S2 (done earlier): polish at static/task-panel-s2.html (S1 frozen for diff): 3-round self-review converged 8→1→0 defects (log in notes/task-panel.md). All 6 S1 open questions resolved: phead paper-tint band, scrim .22, open-scrolled-to-newest, Terminate isolation restored (gap 15→39px via 'AI diagnosis' copy), deck-only ask affordance, cross-read outline dot kept. Textarea autogrow 52→124px + internal scroll; scroll-aware tlbar shadow seam; styled scrollbars; View-transcript pressed state. Stress passed: 2-line title (single-line ellipsis rule + tooltip), 40+ entries (deck pinned, deckBottom==winBottom both sizes), 10-line injection. Zero console errors 1280x800+1440x900 across base/expanded/stress. Shots notes/shots/task-panel-s2-* (final + -r1/-r2). 4 forks logged to DECISIONS-NEEDED."
    - id: m3-conflict-card
      name: "Conflict adjudication card"
      status: completed
      gate: true
      notes: "S1-S5 done. S5: interaction/state suite 14 tests in tests/conflict-interactions.spec.ts — fixture × zone-state MATRIX (all 5 fixtures: grade class+kicker, 2 side rows, symbol rows shown, toggle label +9 more/+1,197 more/none, h4 count 3/12/1.2k/1, diag fresh/stale/empty with '· 3 edits since', placeholder contract, pause-menu 2 rows + noop count, zero console errors). Adjudication actions now DO something (demo stub, design fork iter-13): clicking swaps the footer for an optimistic feedback band — text pill first, neutral tokens only (SENT=done / PAUSING,IGNORED=idle; semantic colors stay reserved), honest future-tense sentence ('queued at their next turn boundary', never 'paused'), visible mono 'demo' marker with the no-live-session disclosure tooltip, focused quiet Close-card button (one keystroke to done; focus returns to the exact opener), role=status aria-live=polite, data-kind = the S3 AdjudicationAction kind (the union is now actually constructed). Inject: typed note / empty+diagnosis ⇒ AI-suggested send-time default / empty+no-diagnosis ⇒ nothing sent, textarea takes focus (fork). Pause: real side ⇒ PAUSING naming both sides, keyboard-driven through the menu; waiting side stays the S2 honest no-op (no feedback — regression-tested). Ignore: one modest INLINE confirm (never a browser dialog), focus on Keep (safe default), Escape ladder pause-menu → confirm → card, menu/confirm displace each other; confirmed ⇒ IGNORED scoped to THIS pair, card stays open. Run/Re-run stub: toggles an honest 'Demo — nothing ran' note (aria-pressed, mono claude -p), no fake progress, verdict/stale untouched, space yielded back (fork). 1200-symbol perf sanity: expand < 3s smoke ceiling (declared diagnostic/tunable), scroll sweep bottom/middle/top with seam-vs-metrics agreement, collapse + SENT still works. Geometry @1280x800+1440x900 in the busiest states: stress (12 expanded + textarea at 124 cap + menu open — modal-in-main, strict stacking, foot pinned, .cbody only scroll region, no page scroll, menu in viewport) then feedback state (band inside footer, footer still pinned, no x-overflow). Seams recompute on footer swap (layout-effect deps + ResizeObserver, asserted against real scroll metrics). Gates: tsc clean; pnpm build green; Playwright FULL 85/85 (71 existing untouched + 14 new, all first-run green). SCALE-EXTREMES closed: per-zone evidence table in notes/conflict-card.md (card/header/grade/between/symbols/diagnosis/adjudication × N=0/1/many, TEXT, NUMBER, scroll perf, SPACE, DYNAMIC, SCREENS — every cell citing code or a test). 5 forks to DECISIONS-NEEDED (iter-13). — S4 (done earlier): Dynamized: src/components/ConflictCard.tsx (zones a/b/c, one dialog) + src/conflict-derive.ts (pure view derivations), consuming ONLY conflictFixtures — zero hardcoded content. Integration: all THREE open paths wired with keyboard parity + focus-return-to-exact-opener (rAF): map sub-block clash chip (SubView.conflictId added in derive.ts), rail CONFLICT pill (pill = own affordance, card body still opens the panel — fork), titlebar conflict stat; close = X/Escape/scrim; Escape with pause menu open closes the MENU first (S2 behavior, tested). Modal exclusivity: conflict card ↔ task panel mutually exclusive (fork); side rows deliver the S2 promise — click opens that task's panel. Reconciled iter-11 fork #2: v8-baseline conflict-osm sharedSymbols = card fixture's names (single source of truth; map shows only the count 3 — zero test edits needed; timestamps NOT reconciled, different snapshots — fork). Global [hidden]{display:none!important} added app-wide (iter-10 3rd-occurrence bug class). All S2 behaviors mechanically verified: seam shadows (scroll+ResizeObserver+expand), +N expand/collapse (1200-symbol fixture expands to exactly 1200 rows, h4 = 1.2k), pause split-menu w/ honest no-op row (enabled, secondary ink, waiting 5m), textarea autogrow 52→124→52, empty-note placeholder contract both states, staleness marker (neutral dot + · 3 edits since), backtick tokens render mono (2 resolve() spans in ui-monospace — iter-11 fork #4), ?conflict= dev param reaches all 5 fixtures (zero console/page errors). Gates: tsc clean; pnpm build green; Playwright 71/71 = 59 existing untouched + 12 new (tests/conflict-parity.spec.ts). Parity: modal element shots pixel-equivalent to the S2 static (red/yellow/empty); deltas = underlay (real map + rail vs simplified canvas), freshness element, mechanical tooltip copy — documented in notes/conflict-card.md §S4. 5 forks to DECISIONS-NEEDED (iter-12). — S3 (done earlier): Types: src/conflict-types.ts extends src/types.ts (Conflict/Task/scopes/git reused, zero duplication), per-field source annotations in the panel-types style; the card's one non-mechanical source (claude -p output) is quoted verbatim + provenance-labeled and confined to zone b. ConflictDiagnosis = verdict + sides tuple (per-side doing rows, taskIds order) + suggested (doubles as the empty-note send-time default per iter-10) + provenance{diagnosedAt, engine:'claude-p-local'} + stalenessEditsSince (PostToolUse EDIT count after diagnosedAt; reads never count; stored not derived — SymbolTouch keeps latest-touch only, deriving would undercount; fork logged). AdjudicationAction union = inject_note{note? — empty ⇒ suggested verbatim, marked AI-suggested} | pause_side{taskId} | ignore_pair. ConflictCardFixture = capturedAt + conflict (map type unchanged) + tasks:[Task,Task] denormalized in taskIds order + ResourceCrumb (denormalized names, standalone render) + SharedSymbolEvidence[] 1:1 with conflict.sharedSymbols (per-symbol [SymbolTouch,SymbolTouch] edit/read provenance; 'both edited'/'w × r' DERIVED from actions, never stored) + diagnosis? (absent = dashed empty state). S1 leftover DECIDED: >999 symbol count follows the map's NUMBER-huge rule via formatCount/exactCount ('1.2k' + exact '1,200' in tooltip). Fixtures (TS literals + satisfies; registry conflictFixtures + conflictFixtureByName in fixtures/index.ts): conflict-osm-red-diagnosed (S2 red verbatim — 8m/31m/9m, 3 symbols with S2 tooltip times, diagnosed 11:12, staleness 0 = last edit), conflict-no-diagnosis (?v=empty variant, shared consts minus diagnosis), conflict-yellow-stale (S2 yellow verbatim — W×R, 12 symbols incl. the 61-char name, long task titles, waiting no-op side 5m, diagnosed 11:02, stalenessEditsSince 3 with the fixture's own touch times as evidence 11:03/11:05/11:08), conflict-1200-symbols (NUMBER-huge/N=many: 1200 deterministic codegen-sweep symbols, ~90-char names every 97th row, staleness COMPUTED from the generated touches so the fixture cannot lie), conflict-one-symbol (N=1 — closes the S1 notes obligation; detected 45s, seconds rung, no diagnosis). Gates: tsc --noEmit clean (strict + exactOptionalPropertyTypes); pnpm build green; Playwright suite still 59/59; runtime invariants spot-checked via throwaway vitest, 6 checks (symbol/task/side order alignment + uniqueness, S2 age reproduction 8m/31m/9m · 31m/1h/5m · 45s, staleness counters == touch-derived counts, 1.2k/1,200 formatting) then deleted. Type→signal table in notes/conflict-card.md. 5 forks to DECISIONS-NEEDED (iter-11: count-abbreviation rule; v8-baseline id-reuse/content divergence, S4 must reconcile; stored staleness counter; backtick-mono rendering; fifth fixture). — S2 (done earlier): Polish at workbench/app-demo/static/conflict-card-s2.html (S1 frozen for diff). Self-review converged 3→0→0 over 3 rounds (log in notes/conflict-card.md): R1 defects = chips hard-cut (inline-flex defeats text-overflow → inline-block), red hero state scrolled 7px @1280 (diag/prov margins tightened one token each → fits both sizes, bodyScroll 0), no scroll cue when .cbody clips (yellow@1280 hid the whole provenance row → scroll-aware seam shadows, task-panel S2 language: grade casts down when scrolled past top, footer casts up while content hides below; refreshed on scroll/ResizeObserver/click/variant-switch, killed by reduced-motion). All 7 S1 open questions RESOLVED + implemented: (1) staleness = neutral prov dot + '· N edits since' marker + honest tooltip (yellow stale — 3 batching edits after its 11:02 diagnosis per its own fixture times; red fresh — diagnosed 11:12 = last edit); (2) red=need tokens ratified iter-9, untouched; (3) empty-note inject = send-time default surfaced in the placeholder ('leave empty to send the Suggested line above, verbatim…', sent marked AI-suggested), prefill rejected as dishonest, setVariant swaps the no-diagnosis card back to the generic placeholder; (4) task rows adopt v8 rail-card hover (translateY -1px + sh-2 — same click-opens-panel affordance, same language); (5) no persistent animation in the modal confirmed (the map keeps the one allowed breathe); (6) pause-menu waiting task = enabled honest no-op at secondary ink (.noop, ink-500); (7) ?dev=0 hides the dev bar + .dev[hidden] guard (display:flex defeats hidden — same S1 .center bug class, 3rd occurrence, S4 should go global [hidden] rule). Stress in polish: 12-symbol expanded holds 660/708@1280 (728/776@1440) with .cbody the only scroll region (header/grade/footer pinned, probe green); verdict 3 lines wraps never truncates; textarea at 124px cap with footer pinned; all-at-once stress scenario (expand+cap+menu open) clean. Gates: standalone render; zero console/page errors × 5 scenarios × 2 viewports × 3 rounds; probes (modal-in-main, foot-pinned, seam-correctness, chip-ellipsis, dev-hidden) green. Shots notes/shots/conflict-card-s2-{red,empty,yellow,yellow-expanded,stress}-{1280x800,1440x900}.png (+-r1/-r2 rounds kept). 6 forks logged to DECISIONS-NEEDED (iter-10). — S1 (done earlier): Static adjudication modal at workbench/app-demo/static/conflict-card-s1.html — centered 640px over blurred+scrimmed map. Three zones per decision-project-020/018: (a) static evidence: CONFLICT pill + resource title + first-detected age (merge/no-re-alert tooltip), territory›sub-block›file crumb, grading strip (soft fill, text-first: RED W×W = need tokens / YELLOW W×R = clash tokens), two task rows with REAL state pills (conflict stays an attribute) + declared w/r chip + branch chip, shared-symbol mono list with per-row provenance tooltips and 3+'+N more' overflow; (b) AI diagnosis: completed state (verdict + per-side rows + Suggested + 'diagnosed by your local claude' provenance + Re-run) and true-empty dashed state with primary-quiet 'Run AI diagnosis' (cost-honesty tooltip: claude -p, no extra key) — both reachable via dev bar or ?v=; (c) adjudication: Inject primary (inline note textarea, autogrow 52→124, solid ink-900 'Inject to both'), 'Pause one side' split-button listing BOTH tasks (no side presumed; Escape/outside-click close), 'Ignore this pair' quiet gray isolated right with permanence tooltip. Variants: red-diagnosed / red-empty / yellow×12-symbols+long-names. Verified headless: 3 variants × 1280x800+1440x900, zero console errors, modal-inside-main geometry probe; shots notes/shots/conflict-card-s1-*.png. Real bug found+fixed: author .center{display:flex} defeated the hidden attribute (both variants stacked) → .center[hidden]{display:none}. Notes + scale-extremes prelims + 7 S2 open questions in notes/conflict-card.md. 3 forks to DECISIONS-NEEDED."
    - id: m4-empty-install
      name: "Empty state / first-run"
      status: completed
      gate: true
      notes: "S1-S5 done. S5: interaction/state suite 19 tests in tests/install-interactions.spec.ts — CTA demo storyboard end-to-end via the CLICK path (timing-tolerant milestone assertions: installing swap → 2-done → 3-done → connected chrome with repo chip/Synced just now/3 zero-groups/launch/guidance, URL follows, zero console errors); failed→Retry→completes (ONLY the failed step reruns, done stays 2, .why gone); Map-this-repo → mapping chip → stop in BOTH placements (Moment B guide + Moment C corner, elapsed mono asserted %-free, footprints untouched through the cycle); footprint tooltip honors 260ms intent (nothing at 80ms; exact count + sampleFiles + mapping promise; instant hide); iter-17 DEBT PAID — rail card AND footprint click → task panel: footprints role=button tabIndex=0, click + Enter/Space open the SAME synthetic panel as the card (one subject two affordances, no footprint-detail surface invented — fork), synthetic honesty asserted (exactly 1 'You · launched' row, tail 'nothing emitted yet'), canvas .veiled + aria-hidden under the scrim, all three close paths (X/Escape/scrim) return focus to the exact opener (selector re-queried, rAF; polled in tests), remount per task; +N earlier sessions chip click DESIGNED (fork): listing popover .fp-pop (aria-haspopup/expanded) — collapsed sessions oldest-first, title ellipsis + exact mono count + footprint tooltip per row, row click opens that task's panel (panel close focuses the CHIP — the stable opener), Escape (focus→chip)/outside-click/re-click all yield the space back, visible blocks do NOT re-pack ('show all at floor' REJECTED as geometrically dishonest — collapse means floors don't fit); correlate-hover card↔footprint implemented per LOOP.md S5 (v8 language): card hover/focus → its fp .lit (ring + scale 1.012) + others AND chip dim .14 (.terr.fpfocus), reverse fp hover → .rail.dim .3 with card .hot at 1, COLLAPSED subject lights the +N chip (its block has no pixels — fork; chip→N cards reverse deliberately not wired), space yields on leave/panel-open; geometry @1280x800+1440x900 on both extremes: nine-footprints 6 blocks in-bounds pairwise-disjoint + chip and open popover in-bounds, tiny-repo cap ≤60%+ε honored + feet legible, no page overflow either axis. Gates: tsc clean; build green; Playwright FULL 122/122 (103 + 19 new, all first-run green); vitest 11/11. SCALE-EXTREMES PROTOCOL CLOSED: per-component evidence table (titlebar / connect card+checklist / rail / territory+guidance / footprints / +N chip+popover / mapping chip / panel-over-install × N=0/1/many, TEXT, NUMBER, SPACE, DYNAMIC, SCREENS) in notes/empty-install.md, every cell citing code or a test. 4 forks to DECISIONS-NEEDED (iter-18). — S4 (done earlier): first-run states dynamized into the App — connection-state layer ABOVE the map render path (App.tsx early-return, map path untouched; main.tsx ?install= reaches all 10 fixtures, unknown names fall through to the map): InstallScreen.tsx routes purely on fixture data (none → ConnectCard.tsx; connecting → InstallChecklist.tsx with statuses/failure verbatim from InstallStep[] incl. failed row + Retry; connected → repo-chip titlebar + zero-groups rail (TaskCard reused via a minimal MapFixture join, territories deliberately empty) + launch + full-bleed UNCATEGORIZED gray + guidance + Map-this-repo / MappingRun status chip (local override makes chip state reachable from every button, click stops the pass) + footprints via packFootprints with +N-earlier-sessions chip on the nine-footprints extreme). Storyboarded hard swap implemented behind the demo CTA click path ONLY (direct ?install= loads stay static): CTA/Enter (autofocus keyboard path) → installing fixture → steps advance (900ms demo pacing, tunable/non-product) → last check lands → 400ms hold (2×t-base) → card exits reverse-cardIn 200ms → connected chrome enters chromeIn 200ms (.first-run.enter scoped, shots unaffected); Retry reruns ONLY the failed step then completes the same storyboard. Parity: 16 shots (8 variants × 1280x800+1440x900) at notes/shots/empty-install-s4-*; connect/installing/install-failed/connected/mapping/first-task pixel-equivalent (floor block reproduces 6/58/24/26 EXACTLY, asserted); the two KNOWN S3 deltas ASSERTED in the spec (two-tasks x-order per the written oldest-first rule; first-task-200 derived 53.7×58.2 vs drawn 58×58); minor deliberate text deltas documented (local <1s Synced-just-now rung; composed-from-data tooltips). Defect found+fixed in review: task panel bare .files{display:none;mono} swallowed the footprint foot (hidden text still satisfies toHaveText — spec now asserts toBeVisible); breathe keyframes renamed breathe-soft to dodge the map collision. S4 open questions resolved: +N chip overlays bottom-right above the foot (fork); pristine checklist renders from PRISTINE_INSTALL_STEPS data (fork). Gates: tsc clean; build green; Playwright FULL 103/103 (85 + 18 new: 8 parity-shot pairs, 10-fixture zero-error sweep, checklist/zero-groups/chip probes, packing bbox probes, CTA + Retry transitions, autofocus, map fall-through); vitest 11/11. 7 forks to DECISIONS-NEEDED (iter-17). — S3 (done earlier): types + fixtures + packing engine. Types: src/install-types.ts extends src/types.ts (Task/scopes/git/RepoInfo/SyncFreshness reused, zero duplication) — RepoConnection = none | connecting{repoPath, steps} | connected{repoPath, repoFiles = git ls-files count at connect (closes S2 open question 1), mappedAt?: never = never-yet enforced at type level}; InstallStep GENERAL per the iter-15 note ({id,label,status pending|now|done|failed, failure?:{reason, codeRef, fix?}} — same row language for a failed MCP/DB step, failure text verbatim from installer stderr); MappingRun = none | running{startedAt} | done{startedAt,finishedAt} (local claude -p process facts; no progress fraction typed because none exists); UncategorizedFootprint = facts only {taskId, filesTouched = PostToolUse paths matching no anchor, firstSeenAt, sampleFiles?} — geometry DERIVED never stored; pre-mapping scopes point at the UNCATEGORIZED_TERRITORY_ID sentinel (fork). Engine: src/install-derive.ts packFootprints, pure + deterministic — areaFrac = clamp(files/repoFiles, 6.24% floor, 60% cap), sqrt damping both axes (120/640 → 41.6×45.1 reproduces the iter-15 worked example), shelf-pack bottom-left/oldest-first/3% gutters/bottom-aligned/wrap-up (insets measured from S2: 6/6/12/16 — N=1 degenerates to S1 rect 6,58,24,26 EXACTLY), overflow ladder = shrink-all-together (binary search largest fitting g on floor-to-natural interpolation, never below floor) then collapse OLDEST to +N chip; helpers footprintFootText/collapsedChipText reuse formatCount (ONE number rule). Fixtures (TS literals + satisfies; registry installFixtures + installFixtureByName in fixtures/index.ts): the 8 S2 variants as data (content verbatim — 150-char path, step states incl. the failed-hooks row with chmod fix, mapping running 2m, tasks 4m/11m/18m with 3/120/200 files, capturedAt 2026-07-12T10:22:00-07:00 like the map) + extremes install-nine-footprints (9 concurrent floor blocks → 6 visible + 3 oldest collapsed, the +N path) + install-tiny-repo (repoFiles=12 → cap 10/12 + near-floor 1/12 + shrink ladder; brief premise corrected — floor cannot dominate at 12 files, 1/12=8.3% > 6.24% floor; fork). Tests: src/install-derive.test.ts, vitest 11/11 first-run green (floor exact, cap area+aspect, sqrt damping incl. doubling→sqrt2, N=1=S1, two-tasks shelf geometry, wrap 4→3+1, shrink-before-collapse, 9→6+3 overflow, determinism/input-order independence, bounds+no-overlap invariants on all fixtures, foot-text number rule) — loop's first pure-logic surface; vite.config.ts scopes vitest to src/**/*.test.ts so Playwright specs stay out. Gates: tsc --noEmit clean; pnpm build green; Playwright FULL 85/85 untouched; vitest 11/11. S4 deltas documented in notes (two-tasks x-order per the written rule vs S2 hand placement; first-task-200 derived 53.7×58.2 vs drawn 58×58). Type→signal table in notes/empty-install.md. 4 forks to DECISIONS-NEEDED (iter-16). — S2 (done earlier): polish at static/empty-install-s2.html (S1 frozen for diff); self-review converged 1→0 defects over 2 rounds (R1: autofocused CTA showed the browser-default blue focus ring → ported the app's system :focus-visible ring from src/app.css; R2: zero new). Variants 5→8: +install-failed (hooks step fails on unwritable ~/.claude/settings.json while MCP+DB complete — ✗ + mono 'failed' in need tokens, reason row with code-chip path + right-aligned Retry that reruns only the failed step, consequence-honest footer '2 of 3 done. Sessions won't report until hooks install'), +mapping (Map-this-repo button yields to '● Mapping this repo · 2m' status chip — breathe dot = that state's one persistent animation, elapsed mono time only, no fake progress %, click stops the pass), +two-tasks (2 concurrent pre-mapping footprints demoing the packing rule). Kernel centering note adjudicated: connect card stays CANVAS-centered (optical centering belongs to the bounded field — rail seam/grid/highlight bound the canvas; window-centering gives 150/450 asymmetric gutters inside the grid field; probe numbers + full argument in notes). All 5 S1 open questions RESOLVED: (1) packing rule for S3 = areaFrac clamp(filesTouched/repoFiles, 6% floor = S1's N=1 block, 60% cap) + sqrt damping, shelf-packed bottom-left in launch order, 3% gutters, bottom-aligned, wrap upward; overflow → shrink-all-to-floor → oldest collapse to '+N earlier sessions' chip; repoFiles = git ls-files count captured at connect; (2) B guidance stays until the first footprint replaces it, no timer; (3) installing→connected = hard-swap storyboard (final check lands → 400ms = 2×t-base hold → card exits reverse-cardIn 200ms → connected chrome enters 200ms), card-to-territory morph rejected; (4) mapping-in-progress chip as above; (5) keyboard path resolved now — CTA autofocuses, Enter-to-connect. Stress: repo path raised 92→150 chars exactly (leading-ellipsis truncation probe green both sizes, used in installing + install-failed). Gates: standalone render, zero console/page errors × 8 variants × 1280x800+1440x900 × 3 rounds; probes (page overflow, per-variant data-show counts, path truncation, footprint-inside-territory, footprint no-overlap, centering geometry) all green. Shots notes/shots/empty-install-s2-*-{1280x800,1440x900}.png (final + -r1/-r2 rounds, 48 files). 6 forks to DECISIONS-NEEDED (iter-15). — S1 (done earlier): static first-run at static/empty-install-s1.html — five dev variants covering the three moments: A connect (no repo — quiet titlebar, dashed empty rail, centered 420px connect card: one ink-900 CTA 'Choose repo folder' + decision-025's three install rows as checklist with per-row tooltips + 'All local — no account, no API key'), A′ installing (CTA→picked repo path with leading-ellipsis truncation on a 92-char path + rows mid-install: check done / pulsing-dot now = the screen's ONE persistent animation / pending), B connected (full-bleed UNCATEGORIZED gray territory + one-sentence ink-500 guidance + quiet 'Map this repo' with claude -p / ~10min cost-honesty tooltip; rail groups collapsed to quiet 0-headers, launch present, zero-count titlebar stats hidden, no legend), C first task (RUNNING card 4m + soft-green footprint block on the gray: task-title kicker + honest '3 files · not yet mapped to features'; Map-this-repo stays reachable as corner button), C×200 (200-file first session — footprint claims most of the gray, count renders plain per the ≥1000 abbreviation rule, exact in tooltip). v8 tokens verbatim, 260ms tooltip JS, global [hidden] guard, everything hover-explainable, CTA click demos A→A′. Verified headless: 5 variants × 1280x800+1440x900, zero console/page errors, probes (no page overflow, variant-visibility counts, path truncation active, footprint-inside-territory); shots notes/shots/empty-install-s1-*.png (10). Notes + scale-extremes prelims + 5 S2 open questions in notes/empty-install.md. 6 forks to DECISIONS-NEEDED (iter-14)."
    - id: m5-menubar
      name: "Menubar dropdown"
      status: completed
      gate: true
      notes: "S1-S5 done, merged per amendment (S1+S2 iter-19, S3+S4+S5 iter-20) — final screen of the five-screen loop scope. S3-S5: PURE ROLLUP over MapFixture, no menubar fixture shape — src/menubar-types.ts + menubar-derive.ts deriveMenubar(fx): counts (zeros hidden), waiting-only badge with 99+ cap (stale = gray/static), freshness + decision-github-002 stale honesty line from sync.lastFetchAt, Needs-you = waiting tasks × conflict PAIRS interleaved OLDEST FIRST (S1 open question 4 encoded; conflict age basis = detectedAt — fork; pair = ONE subject-labeled row '… — 2 writing', sub-block name w/ territory fallback), overflow copy 'and N more waiting…' vs generic 'and N more…' when a conflict hides, quiet line incl. true-N=0, item tip per state, deskClock(capturedAt). vitest 16 new (27/27): busy=v8Baseline rows/ages, interleave with conflict-older case, conflict-only (no badge, never quiet), subject fallback, quiet + nothing-running, stale badge/note/never-fetched, overload 52m/47m/41m + 9-more-waiting, flood 99+/exact 143/total 145/generic more/one-unit 2h-1h ages, cap boundary 99 vs 100, registry uniqueness, deskClock tz-free. S4: MenubarScreen.tsx as a separate render path (App early-return like InstallScreen; ?menubar=busy|quiet|stale|overload|flood, bare ?menubar=1→busy, unknown→map) — desktop context + strip stand-ins + Vibehub item + dropdown, ALL from the rollup; busy IS v8Baseline, quiet/stale/overload/flood = plain MapFixtures in fixtures/menubar-extremes.ts (stale = v8 w/ 47m-old fetch; overload/flood = S1 head items verbatim + deterministic filler, 143 waiting/2 conflicts/31 running); CSS ported under .mbdesk (.div/.foot/.spacer renamed mb-*; .stat/.pill/.fresh + breathe keyframes reused ON PURPOSE — v8 vocabulary); anchor = layout effect + resize, same math as the static. Parity busy/quiet/stale ×1280×800 pixel-equivalent; 5 ASSERTED data-rule deltas (conflict age 8m not 31m; flood one-unit ages; '2 conflicts' plural; capturedAt clock; composed tooltips) in tests/menubar-parity.spec.ts + notes. S5: 34 new Playwright (parity 20 + interactions 14) — item toggle w/ aria-expanded, Escape + outside-click close with rAF focus return to the item, selection semantics (rows/pills/more/footer close like a real menu; intent in tooltips — fork), badge matrix incl. computed animationName breathe/none, click-intent tooltips ×4 variants + pair-naming conflict tip, 260ms intent delay + instant hide, every dropdown text leaf under [data-tip] ×5 variants, keyboard Tab walk stats→rows→more→footer + Enter-select + Escape/Enter reopen re-anchor, geometry @1280×800+1440×900 (anchored, in-viewport, zero page overflow). Gates: tsc clean; build green; Playwright FULL 156/156 (122 + 34 new); vitest 27/27 (11 + 16 new). Scale-extremes closed in notes/menubar.md (N=0 true-zero through flood 145, TEXT ellipsis probe, NUMBER cap boundary, DYNAMIC focus-return, SCREENS both sizes). 5 forks to DECISIONS-NEEDED (iter-20). — S1+S2 (done earlier, iter-19): static/menubar-s1.html: generic macOS desktop + menubar strip (no trademarks, all stand-ins tooltipped), Vibehub item (territory glyph + breathing waiting badge = the one persistent animation; gray/static when stale), 340px dropdown anchored under the item — repo line + freshness, v8 stat-pill counts (zeros hidden), Needs-you top-3 (conflict pair = one subject-labeled row) + 'and N more…' collapse, footer Open Vibehub (compact ink-900) + Start a task. 5 variants: busy/quiet/stale (decision-github-002 honesty line)/overload ×12/flood 99+ badge cap. Self-review 2 rounds → converged 2→0 (UA button border on stat pills; conflict row tail truncation), probes green (anchor, viewport fit, data-show, ellipsis, badge count, tooltip coverage), zero console errors, @2x retina checked. 5 forks logged. Scale-extremes S1-scope answers in notes/menubar.md."
    - id: m6-review-revisions
      name: "Post-close-out revisions from Wayne's live review (rev circle)"
      status: completed
      gate: false
      notes: "A+B+④+⑦ done. rev-2 (iter-22): (④, REVOKES iter-7 scrim-over-rail, quote '左栏不要变灰') rail stays LIVE under an open panel/conflict card — scrim+blur cover the CANVAS only (left = --rail-w; conflict card centers over the canvas; divider z-5 stays resizable); another rail card = ONE-click swap-in-place (remount per task via key, opener re-targets for focus-return); same card/pill = toggle close; correlate suppressed while a modal is open (fork logged — veiled canvas would give half-feedback; focus-return correlate flash ⑤ still fires after close); mutual exclusivity + close paths unchanged; install screen keeps full scrim (empty placeholder rail). (⑦, decision-workbench-003, REVOKES iter-20 waiting-only badge) menubar badge = waiting + conflict PAIRS (a pair counts once); tips enumerate both ('1 waiting · 1 conflict'); stale badge stays gray/static; needs-you rows already subject-first — verified, no other UI change. Gates rev-2: tsc clean, build green, vitest 44/44 (42+2: single-waiting promise, pair-cap boundary), Playwright 170/170 (165+5: live-rail ×4, conflict canvas-scrim/toggle ×1; conflict-parity mutual-exclusivity rewired through the now-clickable rail pill) on DEMO_PORT=5499. All assertion deltas documented in DECISIONS-NEEDED rev-2 entries. rev-1 (iter-21) record: WAYNE VERDICTS 2026-07-12 (rule overrides, ratified — logged in DECISIONS-NEEDED): (A) rail-card chip rows WRAP, all chips visible, cards grow taller; +N demoted to >12-chip pathological fallback (first 11 + +N, deduped enumerating tooltip); branch chips show the FULL branch middle-truncated (middleTruncate() pure fn, 28-char budget sized to the 240px min rail; scope chips keep tail-ellipsis); fixture task-pathological-scopes (15 chips) keeps +N exercised. (B) resizable rail/canvas split: pointer-drag divider (no library, grab-offset-correct), clamp 240–480px, double-click resets 300px, localStorage persistence, keyboard accessible (role=separator + aria-value*, arrows ±16px); net-zero layout at the 300px default so fresh contexts stay v8-identical; chip wrap responds live to rail width (asserted at both clamp extremes). Tests: vitest 27→42 (derive.test.ts: middleTruncate edges + chip rules); Playwright 156→165 (old +8-collapse test → wrap/middle-truncation/pathological trio; rail-resize.spec.ts ×7); all spec URLs route through tests/env.ts DEMO_PORT so suites run on a private port (dev server on 5199 untouched). Gates: tsc clean, build green, vitest 42/42, Playwright 165/165 (DEMO_PORT=5299). Documented parity deltas: third scope-overload rail card; full-branch chips; 3-chip baseline cards may wrap to two chip rows."
    - id: b1-team-visibility-slice
      name: "Backend M1 ①: 团队可见端到端垂直切片(git+gh→SQLite→map,decision-github-003 ② 硬门槛)"
      status: completed
      gate: true
      notes: "workbench/packages/core+cli 诞生。GitFacade(fetch/branch/足迹 diff/merge-tree 预警/gh pr,worktree 归域 github-004)+ SQLite WAL 团队子集(repos/sync_state/team_branches/team_branch_files/team_conflicts,按 repo 分域)+ team-sync + MapFixture 导出 + demo ?fixture=live(缺席诚实回退)。映射语义四条落 decision-workbench-004(draft 待审):冲突候选=unmerged∧(无PR∨open)(020 并发才算撞)、basic 档 unmerged→stalled/PR 收口→done、蒸馏前单一 Uncategorized 地盘、detectedAt 首检保龄。Vibehub 自身 repo 实证零服务端:9 branches/3 unmerged/1 活跃、7 PRs matched、0 活跃冲突(死分支噪音 2 对被过滤)、真数据地图截图验证。Gates: core vitest 42/42、demo vitest 44/44、Playwright 170/170、tsc clean ×3 包。"
    - id: b2-three-domain-schema
      name: "Backend M1 ②: SQLite 三表域 schema(图/运行/配置,五 types 文件直译,decision-025)"
      status: completed
      gate: true
      notes: "migration 002(v1→v2 升级保数据,测试钉死)。运行域 tasks(五态 CHECK)/sessions/events(11 型 union 判别列+JSON verbatim 往返)/footprints/scopes(seq 保申报序)/injections(claim 原子 FIFO 不双发)/conflicts+conflict_symbols(序对齐不变式)/conflict_diagnoses(claude -p verbatim)/notifications(预算账本)/mapping_runs;图域 features(territory/sub-block 同表)/specs(无 D)/anchors/edges;配置域 settings(repo 遮蔽 global)。DERIVED-NEVER-STORED 全线:milestone 档/twist/filesTouched/ordinal/staleness/geometry 不建列,ActivityStore/GraphStore 读时派生(read 不计 staleness、未锚文件=诚实灰区,均有测试)。契约 types ×4 canonical 家入 core/src/contract。Gates: vitest 59/59、tsc clean、build 绿。"
    - id: b3-hook-cli
      name: "Backend M1 ③: `vibehub hook <event>` CLI 心脏 + 真 session 事件流校准 fixture"
      status: completed
      gate: true
      notes: "hook-ingest(core)+ vibehub hook <event>(CLI,恒 exit 0,错误吞 ~/.vibehub/hook.log)。branch=join key 自动捕事(017/024);worktree session branch/toplevel 从 cwd 解析(修 facade 锚主 root 的 bug+回归测试);UserPromptSubmit 首条→launch 后续→user_injection、PostToolUse→edit/read 足迹、Notification→question+waiting、Stop→self_report(transcript 尾 assistant 原文机械解析)+waiting、SessionEnd→done;StateMachine 纯函数,stalled=读时观察不存转移(016 无 daemon);注入原子 claim→additionalContext 输出→入时间线不双发(018/023)。真 claude -p session 校准:launch/足迹 edit+read/self_report/state_transition ×2 全收,形状与契约一致,hook.log 零错。Gates: vitest 72/72、tsc clean、build 绿。"
    - id: b4-demo-real-read
      name: "Backend M1 ④: demo 前端 fixture → 真 SQLite 读(170 e2e 保绿)"
      status: completed
      gate: true
      notes: "vite middleware /live-fixture.json 每请求经 core dist 现场 openDb→exportTeamMapFixture(连续请求 capturedAt 不同=实证真读;Tauri 读路径的 dev 替身,浏览器 bundle 不碰 native);无 DB/未 sync 依次回退静态导出→404→前端警告降级。fixture-export 合并本地 hook 任务:同 branch hooks 档赢(真五态/statusDetail/worktreePath,远端只贡献 PR 事实)、未 push branch 照进、scope 申报优先足迹自陈兜底。e2e 170→172(live boot 两侧分支+未知名回退)、demo vitest 44/44、core 74/74、tsc clean。真环境截图:地图实时显示 M1 三切片分支与 walking-skeleton 的并发冲突。遗留:demo 契约类型副本待 core browser-safe subpath export 后消灭(镜像纪律生效中)。"
    - id: b5-treemap-spike
      name: "Treemap spike: squarified 布局替手调 demoLayout(蒸馏时算一次缓存)"
      status: completed
      gate: false
      notes: "core treemap.ts 纯函数 squarified(面积比例/无重叠/确定性/N=40 无 sliver/10 万倍偏斜,不变式测试钉死)+ migration 003 feature_layouts 缓存 + fixture-export 接线(蒸馏地盘真布局、灰区按未锚文件加权、occupancy 逐地盘 rollup、申报 scope 指向蒸馏地盘不再归灰)。合成 9 地盘视觉验证:比例正确近方形 respect 92.5% 底线。边界留 M2:灰区缓存(无 feature 行,帧时现算)、sub-block packing。core vitest 88/88。"
    - id: m2-claude-integration
      name: "M2: Claude Code 集成层 — hook 矩阵 + 微协议 + MCP 三组工具 + 注入投递端"
      status: in_progress
      gate: true
      notes: "006–014 已裁:skill 独占 intelligence,hooks=when,MCP/CLI/core=版本化确定性原语+质量闸;headless plugin/hooks/skills/MCP/CLI+SQLite 独立成立,App 是可选 reader/intervention client。Productionization 已覆盖 init/doctor、KB v2、42-operation dispatcher、resumable distillation、progressive skills、repo-safe task identity、原子 hook 投递、真实 App controller/receipts、v8 spatial-workbench UX truth 与 representative artifact/isolated gates。2026-07-14 B7 全矩阵:build/typecheck;unit 301/301(Core 204,CLI 23,MCP 15,App 59);production main.tsx E2E 25/25;historical E2E 171/171;bundle/artifact/dogfood;isolated full matrix 全绿。最终对抗审查补齐 malformed hook flags 的真实子进程 fail-open 回归并批准。仍 in_progress:经明确外发授权后的真实 Claude dogfood;Tauri 打包后置。"
    - id: m2p1-production-shape
      name: "M2 productionization ①:headless repo/app/contracts 正式形态 + fixtures 隔离"
      status: completed
      gate: true
      notes: "app-demo→正式 workbench/app 并入 root workspace;production entry 与 test harness/fixtures 物理分离,43MB 重复截图与废弃 static/notes 清退;App 类型镜像删除,统一 browser-safe @vibehub/core/contracts;Map/Panel/Conflict/Install roots 与 layout/export/CLI 全改正式 snapshot 命名;production graph gate 覆盖动态 chunk 且有负例。Core 125/125,App 44/44,typecheck/build/harness build/bundle scan 绿;独立 task review clean。Playwright 全套留最终综合验证。"
    - id: m2p2-strict-real-boot
      name: "M2 productionization ②:真实 SQLite 默认启动 + typed setup/error,零 dummy fallback"
      status: completed
      gate: true
      notes: "RuntimeService.readWorkbenchSnapshot + production WorkbenchBridge 已接:显式 VIBEHUB_REPO 经 GitFacade canonicalize(相对路径/symlink/worktree→主 repo root),Vite host 仅 loopback dev/test/dogfood,无 cwd fallback/daemon/static JSON/canned fallback;hostless build=bridge_unavailable,DB missing/repo uninitialized/unsynced/internal error 分型可恢复,ok 才渲染真实 map。root workbench:dev/build/test 已加。Core 131 tests,App 49 tests,typecheck/build/boundary 绿;独立 task review clean。loopback E2E 因 sandbox EPERM 留最终 escalated 验证。"
    - id: m2p3-live-read-intervention
      name: "M2 productionization ③:真实 panel/timeline/conflict read model + InjectionQueue 介入"
      status: completed
      gate: true
      notes: "RuntimeService 真实 panel/timeline/conflict read models 已接 SQLite/Git；degradation warning 与 fatal error 分离，basic conflict 明确 evidence_unavailable。事务介入支持 inject/pause/inject-both/ignore，(repo_id,request_id) 幂等、canonical pair 跨 local/team 忽略、queue/claim 语义不冒充 delivery；bridge 请求及完整嵌套 DTO 双向 runtime validation。Core 141 tests、App 55 tests、typecheck/build/boundary 绿；v6→v7 数据保全与双连接并发 claim 有实证；独立 review approved。"
    - id: m2p4-init-dogfood
      name: "M2 productionization ④:vibehub init + 正式文档 + 本 repo dogfood/bundle gate"
      status: completed
      gate: true
      notes: "RuntimeService init/doctor、受管资源 manifest/checksum/ownership/atomic repair、CLI init/doctor/snapshot、standalone workbench workspace/docs/license 已完成。clean-HOME packaged CLI/hook/MCP/native SQLite、isolated subtree copy、真实 App bridge transport、init×2→scope/record/query→snapshot→介入→Stop→doctor dogfood 全绿。symlink 越界/全 manifest preflight/doctor 只读由独立 review 复现后修复并批准。Core 150、MCP 5、App 57。"
    - id: m2p5a-kb-v2-migration
      name: "M2 productionization ⑤A:Feature Room ontology v2 + additive import/read parity"
      status: completed
      gate: true
      notes: "migration 008 additive v2 完成:canonical KB immutable revisions/evidence 与 mapping version 双层 truth、lossless legacy import/audit/quarantine、synthetic finalized active mapping、snapshot parity、legacy 表 DB-level freeze。active-only/sourceKind、repo-scoped lifecycle、cycle/path/layout/hierarchy edge cases 已经独立 review 修复并批准。Core 157、MCP 6、App 57。"
    - id: m2p5b-kb-operations
      name: "M2 productionization ⑤B:KnowledgeService + dispatcher + CLI/MCP parity"
      status: completed
      gate: true
      notes: "KnowledgeService canonical reads/facets/history/evidence/anchors/relations/review 与 draft batch/lifecycle/amend/supersede 完成；17 operations 严格 runtime schemas，共享 OperationDispatcher envelope，CLI/MCP 对称 adapters，跨 worker idempotency 与 SQL 全局 ranked/bounded search 有实证。GraphStore 退为只读 facade，legacy writes 明确 unsupported。独立 review 经 11 项修复后批准。Core 156、CLI 2、MCP 6、App 57。"
    - id: m2p5c-distillation-runs
      name: "M2 productionization ⑤C:versioned/resumable DistillationService"
      status: completed
      gate: true
      notes: "migration 010 + DistillationService 完成:resolved baseCommit/hash inventory、exact leaf scopes、lease generation/expiry、immutable candidates、finding-scoped audited correction、reconcile partition、finalize frozen projections/checksums/anchor diffs、CAS-only activate/rollback；全 mutation receipts 与 CLI/MCP parity。独立 review 经 12 项契约修复后批准。Core 177、CLI 4、MCP 8、App 57。"
    - id: m2p5d-skill-intelligence
      name: "M2 productionization ⑤D:progressive skill stdlib/scripts + ingest/query/distill/update/review"
      status: completed
      gate: true
      notes: "按 fr-* ontology/lifecycle、谨慎吸收 ShadowRepo cold-start mechanics，完成 shared stdlib、五个 progressive skills、确定性 CLI wrappers、42-operation schema v4 input contracts 与 packaged validator。新增 explicit candidate/version reads、Git-tree-pinned true incremental carry-forward/rename/delete、combined bounded capture、strict canonical Unicode/path contract、Knowledge/Distillation direct-service guards。独立 review 经多轮 P1 修复后批准；Core 183、CLI 14、MCP 8、App 57，isolated artifact/dogfood 全绿。"
    - id: m2p5e-forward-test
      name: "M2 productionization ⑤E:fresh-agent forward tests + global package gate"
      status: completed
      gate: true
      notes: "Fresh ingest/query、TypeScript+Python cold-start 与 incremental cases 通过；uncovered files 保持 explicit unresolved，relation semantics、truth-layer isolation、CAS/rollback 通过。Forward tests 发现并修复 global request-ID binding（含 failures/concurrency）、mixed-case/Unicode inventory ordering、Git nonregular modes、pagination/review lifecycle、typed init error、MCP fallback-ID integration。最终 local + isolated verify 通过：Core 191、CLI 20、MCP 9、App 57，bundle/artifact/dogfood green。"
    - id: m2p5f-integrity-review-fixes
      name: "M2 productionization ⑤F:whole-branch integrity review fixes"
      status: completed
      gate: true
      notes: "Merge-blocking integrity wave:repository receipt 绑定 actor/task/input 且 validation/unsupported/typed errors 可重放；MCP v0.2 移除必失败 kb_record/kb_apply_distillation,canonical operation adapters 有成功 fixture；migration 014 为 intervention receipt backfill canonical input hash并返回 typed conflict；managed assets 跨 target+ledger commit 支持 deterministic mid-rename fault rollback,新装/修复均无 debris。验证证据见 .superpowers/sdd/whole-branch-fix-report.md。"
    - id: m2p5g-audit-fix-closure
      name: "M2 productionization ⑤G:boundary-driven audit fix closure"
      status: completed
      gate: true
      notes: "独立审计 B1–B6 已按边界收口:task identity 以 repo-qualified opaque ID 隔离;hook claim/事件/状态在 immediate transaction 内原子化且 transcript I/O 在锁外;live scope matcher 参数方向修正;production main.tsx controller、typed intervention receipts 与 race/focus/modal/reduced-motion 行为有实测;decision-workbench-014 固定 v8 spatial-workbench 权威并移除虚假 affordance;isolated/artifact gates 执行真实 production entry、受管 hook/MCP 命令、corruption negative proofs 与完整 dogfood。B7 fresh verification:build/typecheck,unit 301/301,production E2E 25/25,historical E2E 171/171,bundle/artifact/dogfood,isolated full matrix,diff-check 全绿;最终对抗审查发现并修复 malformed hook flags 在 catch 前 exit 2,真实子进程钉死 hook exit 0 且非 hook 仍 exit 2,复审批准。真实 Claude 外发 dogfood 未执行且不冒充完成。"
    - id: m2p6a-workflow-receipt-contract
      name: "M2 activation ⑥A:typed workflow receipt contract + deterministic evidence projection"
      status: completed
      gate: true
      estimated_files:
        - "workbench/packages/core/src/contract/workflow-receipt.ts"
        - "workbench/packages/core/src/contract/index.ts"
        - "workbench/packages/core/test/workflow-receipt.test.ts"
      estimated_lines: 180-260
      depends_on: []
      complexity_flags:
        - "design_decisions"
      open_questions: []
      description: |
        冻结 setup/query/ingest/distill/inject/checkpoint 共用的 activity、phase、
        outcome、visibility、evidence 与 next-action 语言。receipt 只投影既有
        operation/runtime/hook 事实，不建立第二套 workflow truth store。
      notes: "WorkflowReceiptV1 browser-safe contract + runtime projectors 完成；42 operations 共用 canonical activity/effect map，operation/init/doctor/injection/claim 证据同构校验，update/review vocabulary 补齐。TUI renderer 有 five-section budget、ANSI/control 清理、CJK/emoji display-width wrap；intervention idempotent replay 保留原 outcome + replayed=true。独立 review 两轮修复后 APPROVED。Controller gates: Core 220/220、App 59/59、两端 typecheck、core build、diff-check 全绿。"
    - id: m2p6b-repo-activation
      name: "M2 activation ⑥B:repo activation primitives + managed instruction block + CLI"
      status: completed
      gate: true
      estimated_files:
        - "workbench/packages/core/src/project-activation.ts"
        - "workbench/packages/core/src/runtime-service.ts"
        - "workbench/packages/cli/src/main.ts"
        - "workbench/packages/core/test/project-activation.test.ts"
        - "workbench/packages/cli/test/setup-cli.test.ts"
      estimated_lines: 350-500
      depends_on:
        - "m2p6a-workflow-receipt-contract"
      complexity_flags:
        - "error_handling_heavy"
        - "design_decisions"
      open_questions: []
      description: |
        在现有 init/doctor 上增加 setup inspect/apply/status 与幂等 managed-block
        primitive，保留用户 AGENTS.md/CLAUDE.md 内容并诚实区分 Installed、
        Connected、Activated。非 Git、marker 冲突、symlink、权限和 partial
        failure 均返回可恢复的 typed result。
      notes: "独立 project-activation primitive + RuntimeService/CLI setup inspect|apply|status 完成。AGENTS.md/CLAUDE.md 仅拥有稳定 managed block，双文件全批 preflight、stage/rename、显式 commit point、精确 rollback 与 recovery backup；non-Git、marker、UTF-8、symlink/nonregular、TOCTOU mode、空/坏 manifest 均 typed 且零越权写。DB identity 保持 common repo root，instruction target 绑定当前 worktree toplevel。Installed 由非空健康 release manifest 证明；Connected 要求 current instructions 之后同 checkout 的真实 hooks-tier session；Activated 再要求 handshake 之后 operation-specific 有效 query/ingest receipt，时间门严格 >。独立 reviewer 三轮对抗修复后 APPROVED。Controller gates: Core 243/243、CLI 28/28、两端 typecheck/build、diff-check 全绿。"
    - id: m2p6c-setup-skill-claude
      name: "M2 activation ⑥C:vibehub-setup skill + Claude onboarding vertical slice"
      status: in_progress
      gate: true
      estimated_files:
        - "workbench/skills/vibehub-setup/SKILL.md"
        - "workbench/skills/vibehub-setup/references/onboarding-contract.md"
        - "workbench/skills/vibehub-setup/references/claude-code.md"
        - "workbench/skills/vibehub-setup/references/recovery.md"
        - "workbench/packages/cli/test/skill-package.test.ts"
      estimated_lines: 260-380
      depends_on:
        - "m2p6b-repo-activation"
      complexity_flags:
        - "prompt_engineering"
      open_questions: []
      description: |
        skill 编排 inspect/apply/doctor/activation，fresh repo 进入自然积累，
        existing repo 显式推荐且允许跳过 cold-start distill。用真实 Claude
        session 证明 restart/handshake/query-or-ingest activation，不把 workflow
        intelligence 拆回 hooks 或 MCP descriptions。
      notes: "Implementation checkpoint 完成：vibehub-setup skill + onboarding-contract/claude-code/recovery progressive references + OpenAI metadata；skill 固定 inspect→validated apply→status，所有 setup exit 0/1 解析 typed JSON，partial/blocked 立即停并保留 backup evidence。fresh/existing/ambiguous 由 tracked substantive files + history 的 skill-owned semantic classifier 分流；distill 可跳过，empty query/fake ingest 不得 Activated。Package validator 固定 canonical 六 entry 并拒绝第七项；artifact 使用 installed bin 在 clean HOME/temp repo 验证 inspect→apply→unchanged→current→pre-handshake waiting。双 reviewer修复后 APPROVED。Controller gates: CLI 31/31、typecheck/build、package validator、network-enabled representative artifact、diff-check 全绿。Milestone 保持 in_progress：真实 Claude host restart/handshake/query-or-ingest dogfood 仍需用户提供 target path 与外发授权。"
    - id: m2p6d-knowledge-checkpoint
      name: "M2 activation ⑥D:task-scoped periodic knowledge checkpoint"
      status: pending
      gate: true
      estimated_files:
        - "workbench/packages/core/src/db.ts"
        - "workbench/packages/core/src/hook-ingest.ts"
        - "workbench/packages/core/src/operation-dispatcher.ts"
        - "workbench/packages/core/test/hook-ingest.test.ts"
        - "workbench/packages/core/test/knowledge-service.test.ts"
      estimated_lines: 220-340
      depends_on:
        - "m2p6a-workflow-receipt-contract"
      complexity_flags:
        - "error_handling_heavy"
      open_questions: []
      description: |
        UserPromptSubmit 只做 prompt-id 去重与 task-scoped cadence；达到阈值时
        注入短 checkpoint，成功 canonical knowledge mutation 后机械重置。
        不解析 token/transcript，不在 hook 内判断 durable semantics，并保持
        pause/injection 的现有严格优先级。
    - id: m2p6e-codex-activation-parity
      name: "M2 activation ⑥E:Codex workbench-native activation parity"
      status: pending
      gate: true
      estimated_files:
        - "workbench/skills/vibehub-setup/references/codex.md"
        - "workbench/skills/vibehub-setup/agents/openai.yaml"
        - "workbench/packages/cli/src/managed-assets.ts"
        - "workbench/scripts/verify-plugin-artifact.mjs"
      estimated_lines: 220-340
      depends_on:
        - "m2p6b-repo-activation"
        - "m2p6c-setup-skill-claude"
      complexity_flags:
        - "first_of_kind"
        - "error_handling_heavy"
      open_questions: []
      description: |
        Codex 适配调用同一 activation primitives、SQLite、repo identity 与
        receipt contract；能力缺口显式 degradation，不以 hack 伪造 Claude
        hook parity，也不生成第二套 instruction/state vocabulary。
    - id: m2p6f-presentation-rollout
      name: "M2 activation ⑥F:cross-workflow presentation rollout"
      status: pending
      gate: true
      estimated_files:
        - "workbench/skills/_stdlib/reporting.md"
        - "workbench/skills/vibehub-query/SKILL.md"
        - "workbench/skills/vibehub-ingest/SKILL.md"
        - "workbench/skills/vibehub-distill/SKILL.md"
        - "workbench/app/src/"
      estimated_lines: 300-480
      depends_on:
        - "m2p6a-workflow-receipt-contract"
        - "m2p6c-setup-skill-claude"
        - "m2p6d-knowledge-checkpoint"
      complexity_flags:
        - "prompt_engineering"
        - "design_decisions"
      open_questions: []
      description: |
        setup/query/ingest/distill/inject/checkpoint 统一使用 silent/brief/expanded
        receipt 语言；Claude/Codex 走宿主文本 affordance，App 只富渲染同一事实。
        最终 headless、App-off、fresh/existing 与双宿主 dogfood 全矩阵收口。
  commits:
    - hash: "00bf5b4"
      date: "2026-07-18"
      message: "[21-workbench] feat: add skill-first project setup workflow"
      files_changed: 11
      specs_affected:
        - change-2026-07-18-setup-skill (created, active implementation checkpoint)
      milestones_affected:
        - m2p6c-setup-skill-claude (implementation checkpoint; real Claude dogfood pending)
    - hash: "63634c3"
      date: "2026-07-18"
      message: "[21-workbench] feat: add repo activation and setup CLI"
      files_changed: 9
      specs_affected:
        - decision-workbench-015 (implemented repo activation and onboarding evidence primitives)
        - decision-workbench-016 (typed setup result semantics)
        - change-2026-07-18-repo-activation (created)
      milestones_affected:
        - m2p6b-repo-activation (→ completed)
    - hash: "1f2824d"
      date: "2026-07-18"
      message: "[21-workbench] feat: add deterministic workflow receipt contract"
      files_changed: 16
      specs_affected:
        - decision-workbench-015 (draft → active; onboarding contract)
        - decision-workbench-016 (draft → active; interaction and presentation protocol)
        - intent-workbench-003 (draft → active; implementation intent)
        - change-2026-07-18-workflow-receipt-contract (created)
      milestones_affected:
        - m2p6a-workflow-receipt-contract (→ completed)
    - hash: "6cdea81"
      date: "2026-07-14"
      message: "[21-workbench] fix(audit/b6): make isolated and artifact gates representative"
      files_changed: 9
      specs_affected: []
      milestones_affected:
        - m2p5g-audit-fix-closure (B6 completed)
    - hash: "8d7440c"
      date: "2026-07-14"
      message: "[21-workbench] fix(audit/b5): align UX with approved v8 design truth"
      files_changed: 14
      specs_affected:
        - decision-workbench-014 (created, active; v8 spatial-workbench authority)
      milestones_affected:
        - m2p5g-audit-fix-closure (B5 completed)
    - hash: "9e250bd"
      date: "2026-07-14"
      message: "[21-workbench] fix(audit/b4): harden the production workbench controller"
      files_changed: 11
      specs_affected: []
      milestones_affected:
        - m2p5g-audit-fix-closure (B4 completed)
    - hash: "8843a27"
      date: "2026-07-13"
      message: "[21-workbench] fix(audit/b3): classify live scope evidence correctly"
      files_changed: 2
      specs_affected: []
      milestones_affected:
        - m2p5g-audit-fix-closure (B3 completed)
    - hash: "94bbceb"
      date: "2026-07-13"
      message: "[21-workbench] fix(audit/b2): preserve intervention delivery on hook failure"
      files_changed: 4
      specs_affected: []
      milestones_affected:
        - m2p5g-audit-fix-closure (B2 completed)
    - hash: "093c88a"
      date: "2026-07-13"
      message: "[21-workbench] fix(audit/b1): isolate task identity by repository"
      files_changed: 10
      specs_affected: []
      milestones_affected:
        - m2p5g-audit-fix-closure (B1 completed)
    - hash: "d270f5e"
      date: "2026-07-12"
      message: "[21-workbench] feat(m2): skill intelligence layer + Claude plugin/MCP spine"
      files_changed: 49
      specs_affected:
        - decision-workbench-006 (draft → active; hook matrix + Stop official wire format)
        - decision-workbench-007 (draft → active; B1/B2/B3 + locus micro-protocol)
        - decision-workbench-008 (draft → active; MCP six deterministic capabilities)
        - decision-workbench-009 (draft → active; plugin three-part intelligence boundary)
        - decision-workbench-010 (created, active; skill intelligence ownership)
        - change-2026-07-12-m2-skill-intelligence-spine (created)
      milestones_affected:
        - m2-claude-integration (in_progress; engine spine checkpoint complete, real dogfood pending)
    - hash: "799eb2b"
      date: "2026-07-12"
      message: "[21-workbench] feat(m2): Claude Code 集成层第一批 — 设计块 A/B/C draft + 注入投递端/里程碑启发式"
      files_changed: 15
      specs_affected:
        - decision-workbench-006 (created, draft — 卡点1)
        - decision-workbench-007 (created, draft — 卡点2)
        - decision-workbench-008 (created, draft — 卡点3)
        - change-2026-07-12-m2-integration-design (created)
        - decision-workbench-001 (anchor added — 机械兜底实装)
      milestones_affected:
        - m2-claude-integration (created → in_progress)
    - hash: "7fda323"
      date: "2026-07-12"
      message: "[21-workbench] chore: 截图字节级刷新 — treemap spike 会话 re-render 残留"
      files_changed: 6
      specs_affected: []
      milestones_affected: []
    - hash: "4269a49"
      date: "2026-07-12"
      message: "[21-workbench] feat(treemap): squarified 布局替手调 demoLayout — 蒸馏时算一次缓存"
      files_changed: 11
      specs_affected:
        - change-2026-07-12-treemap-spike (created)
      milestones_affected:
        - b5-treemap-spike (→ completed)
    - hash: "53c834d"
      date: "2026-07-12"
      message: "[21-workbench] feat(backend-m1): demo 切真 SQLite 读 — vite middleware 现场导出 + 本地任务合并"
      files_changed: 12
      specs_affected:
        - change-2026-07-12-demo-live-read (created)
      milestones_affected:
        - b4-demo-real-read (→ completed)
    - hash: "03ef9dd"
      date: "2026-07-12"
      message: "[21-workbench] feat(backend-m1): vibehub hook CLI 心脏 — 写事件→查注入→退出,真 session 校准通过"
      files_changed: 9
      specs_affected:
        - change-2026-07-12-hook-cli-heart (created)
      milestones_affected:
        - b3-hook-cli (→ completed)
    - hash: "33876f3"
      date: "2026-07-12"
      message: "[21-workbench] feat(backend-m1): 三表域 schema 直译 — migration 002 + ActivityStore/GraphStore"
      files_changed: 14
      specs_affected:
        - change-2026-07-12-three-domain-schema (created)
      milestones_affected:
        - b2-three-domain-schema (→ completed)
    - hash: "5cc6a1f"
      date: "2026-07-12"
      message: "[21-workbench] feat(backend-m1): team-visibility vertical slice — core+cli 诞生,git+gh→SQLite→map 端到端实证"
      files_changed: 31
      specs_affected:
        - change-2026-07-12-team-visibility-slice (created)
        - decision-workbench-004 (created, draft)
        - decision-workbench-001 (draft → active)
        - decision-workbench-002 (draft → active)
        - decision-workbench-003 (draft → active)
      milestones_affected:
        - b1-team-visibility-slice (→ completed)
