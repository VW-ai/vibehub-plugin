spec_id: "decision-project-014"
type: decision
state: superseded
intent:
  summary: "当前 source of truth = SQLite；文件只是 projection/export，直至另有正式 supersede"
  detail: |
    当前实现以 SQLite 承载 semantic knowledge 与 operational state，并由 core
    提供唯一读写门、schema migration、query/index 与 projection。

    本决策记录 2026-07-20 以前的 SQLite-only authority。lossless round-trip、
    Git merge、branch/ref cache 与 provenance identity 已完成验证；Wayne 随后
    批准 decision-project-028。SQLite 继续承载 operational truth 和可重建
    semantic cache，但不再是已迁移仓库的 durable semantic authority。
constraints:
  - kind: must
    rule: "当前所有 canonical knowledge mutation 继续通过 SQLite-backed deterministic operations"
  - kind: must
    rule: "任何 source-of-truth 切换必须显式 supersede 本决策"
  - kind: must_not
    rule: "不得让导出文件产生 SQLite 中不存在的 canonical truth"
indexing:
  type: decision
  priority: P0
  layer: epic
  domain: "storage"
  tags: [sqlite, source-of-truth, projection, migration-boundary]
provenance:
  source_type: manual_input
  confidence: 1.0
  source_ref: "Wayne architecture decision — 2026-07-08; status reaffirmed 2026-07-19"
  migration:
    from_repository: "VW-ai/Vibehub"
    from_path: "META/00-project-room/specs/decision-project-014.yaml"
    migrated_at: "2026-07-19"
relations:
  - target: "intent-project-004"
    type: challenged_by
    detail: "The exploration completed and produced decision-project-028."
  - target: "decision-project-028"
    type: superseded_by
    detail: "Explicitly superseded after technical review approval on 2026-07-20."
anchors:
  - file: "packages/core/src/db.ts"
  - file: "packages/core/src/knowledge-service.ts"
  - file: "packages/core/src/distillation-service.ts"
