# app-demo is a standalone demo project — do NOT hoist into the monorepo
# workspace (keeps root pnpm-lock.yaml untouched; drops into a Tauri WebView
# shell later without workspace assumptions).
ignore-workspace=true
