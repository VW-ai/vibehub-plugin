{
  "acceptance": [
    {
      "acceptance_id": "refine-dsh-plugin-after-dual-harness-contract",
      "criterion": "Placeholder: after the dual-harness contract succeeds and this later carrier is scheduled, replan this Ticket to firm with exact supported DSH versions, Profile install, start, restart, upgrade and remove, Slot and Theme composition, DSH-native Chat and Session behavior, the complete Task, Context and Evidence loop, shared-core parity, no Codex runtime dependency, no duplicate Agent loop, truthful capability fallback and public Plugin packaging acceptance."
    }
  ],
  "constraints": [
    "Do not gate the Codex-first product or preview on this Ticket.",
    "Do not require Codex app-server in the DSH package or route one action through both DSH and Codex Agents.",
    "Do not fork DSH upstream, couple VibeHub Tasks to Agent Team tasks or duplicate the Task store.",
    "Reuse shared core packages and keep DSH version translations bounded inside the DSH adapter."
  ],
  "context": "DSH remains strategically useful for distribution and its plugin ecosystem and follows the first daily-usable Codex carrier in delivery priority. It is nevertheless an independent product carrier: ordinary Chat and Agent execution come from DSH Sessions, models, tools and permissions, not Codex app-server. Replan this Ticket after the dual-harness capability contract establishes the shared core; schedule its implementation after the Codex preview as a product priority, not as an execution prerequisite.",
  "context_refs": [
    {
      "purpose": "Binding DSH-only carrier and shared-core architecture.",
      "ref": ".vibehub/rooms/product/decision-vibehub-core-supports-codex-and-dsh-harness-bases.yaml"
    },
    {
      "purpose": "Binding DSH-native runtime ownership boundary.",
      "ref": ".vibehub/rooms/product/decision-dsh-runtime-opaque-plugin-boundary.yaml"
    },
    {
      "purpose": "Accepted DSH-native Profile, Session, command and extension-seam baseline.",
      "ref": ".vibehub/outcomes/ticket-spike-deepseek-harness-foundations.yaml"
    },
    {
      "purpose": "Completed DSH Bundle and Task execution baseline.",
      "ref": ".vibehub/outcomes/ticket-build-task-harness-vertical-slice.yaml"
    }
  ],
  "deliveries": [],
  "kind": "ticket",
  "maturity": "draft",
  "outcome": "Existing DSH users can install a VibeHub Plugin that uses DSH as its complete base harness while sharing the same VibeHub Task and Context product core, with no Codex dependency, upstream fork, duplicate Agent loop or second Task store.",
  "provenance_refs": [
    "conversation:2026-08-20-owner-defers-dsh-compatibility",
    "conversation:2026-08-21-owner-requires-dual-harness-product"
  ],
  "relations": [
    {
      "rationale": "The DSH-only package must consume the accepted shared-core and harness capability boundary instead of inheriting Codex runtime assumptions.",
      "target_ticket_id": "ticket-design-dual-harness-core-capability-contract",
      "type": "depends_on"
    }
  ],
  "schema_version": 2,
  "ticket_id": "ticket-package-dsh-compatibility-plugin"
}
