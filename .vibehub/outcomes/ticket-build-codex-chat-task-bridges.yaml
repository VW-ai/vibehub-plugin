{
  "accepted_acceptance_ids": [
    "origin-provenance-is-canonical-and-immutable",
    "create-task-from-finalized-message-is-exact",
    "attach-and-quote-into-task-are-explicit",
    "remember-is-a-governed-proposal",
    "task-workspace-returns-to-source",
    "bridge-writes-are-explicit-recoverable-and-uncommitted",
    "bridge-surfaces-hold-the-baseline"
  ],
  "closed_at": "2026-08-23T02:12:37Z",
  "evidence_ids": [
    "origin-field-added-validated-and-immutable",
    "create-task-finalized-only-with-exact-origin",
    "attach-and-quote-into-task-explicit",
    "remember-governed-uncommitted-context",
    "origin-chip-return-to-source-and-provenance-edge",
    "bridge-write-class-and-recovery-without-second-store",
    "bridge-surfaces-baseline-and-hookprompt"
  ],
  "kind": "ticket_outcome",
  "schema_version": 1,
  "status": "successful",
  "summary": "Independent adjudication at HEAD 8fc7e6b on codex/deepseek-harness-v1, clean tree, Codex 0.149.0 installed and pinned. Reproduced by the adjudicator: npm test 267 tests, 265 pass, 0 fail, 2 DSH skips; npm run verify exit 0 (artifact 62 files); npm run guard:codex default fixture mode 79/79 wide and 80/80 narrow in Light and Dark with bridge writes verified 7/7 on all six frames and the lifecycle walk 6/6; the same driver with --runtime real --frames none against a disposable bound repository passed the lifecycle walk 6/6 on the installed Codex 0.149.0 (the two Threads it created were deleted afterwards through thread/delete). Per criterion: (1) ticket.schema.json carries exactly one optional origin object on schema_version 2 with no new kind; validateTicketOrigin mirrors it so ticket validate, project validate and buildUiSnapshot reject a malformed origin on disk; assertOriginImmutable runs inside applyTickets before any write, refusing changed or removed origin (origin_immutable) and origin added to a checked-in Ticket (origin_cannot_be_added) atomically for the whole batch; the host writes Tickets only through applyTickets; test/ticket-origin.test.mjs asserts all of this with real assertions and the 106 checked-in Tickets validate unchanged. (2) renderAgentMessage offers the three actions only on finalized items of settled Turns and never on user messages; the guard proves placement against live-Turn and streamed items, previewCreateTask packetText equals the sheet pre byte for byte, and the host test proves previewed packetText equals readTask packetText, startTask payloadText and the thread/read replay; one draft Ticket is written through applyTickets with origin and one placeholder acceptance, no thread/start or thread/name/set is issued, the Graph lists it DRAFT REFINE with the inline marker at the origin Turn, and unbound actions are disabled with the reason in title, aria-describedby and a visible hint. (3) attachTask appends exactly one codex-thread:<thread_id>/turn:<turn_id> reference idempotently (second call added false, file mtime unchanged), never touches origin, deliveries, acceptance or relations, refuses 409 task_closed and 404 task_not_found; relationCounts stay 0:0 and no relation is created; Quote into Task reaches the app-server only as conversation.humanMessage inside the packet, on Start and on a later Turn, and writes nothing. (4) remember lists only canonical Rooms, pins the type to the Context enum, writes one state active Context through putContext with source.ref and one evidence entry, answers 409 room_missing without creating a Room, and the path is uncommitted with the commit count unchanged. (5) origin chip with Return to source focuses the exact origin item on the chat route; markers list every Task of a Turn; sidebar, attention, inbox and Graph cards show origin; the provenance edge is path[data-edge-kind=provenance] drawn for the focused card else the last opened Task, and depends_on path counts and data-relation-counts are unchanged. (6) explicitChatBridge names exactly .vibehub/tickets/<ticket_id>.yaml and .vibehub/rooms/<room_id>/<context_id>.yaml with commits false in the launcher envelope, /health and bootstrap; every git invocation in the host, vh-ui.mjs and vh.mjs is read-only (rev-parse, status, ls-tree, hash-object, merge-base); a Start whose thread/name/set or turn/start is dropped leaves the Ticket valid with nextAction REFINE; origin and attached associations read identically after a launcher restart with no ignored file or store; the static no-second-store test covers the bridge. (7) the three bridge dialogs join the shared Tab trap and Escape handler and restore focus to their trigger; the keyboard audit runs inside each sheet and over the provenance Graph; the contrast test models the new rules and passes in both themes; the hookPrompt divider reads Repository instructions with fixture coverage and no Project instructions string remains in the app sources. Negative control reproduced by the adjudicator on a scratch tree with the app files reverted to ee6afa3 and the new guard kept: the guard FAILS with exit 1 at 1280x800 (65/68 pass) and 390x844 (66/69 pass) in Light, failing hookPrompt divider, finalized-only placement and the whole bridge flow (thrown at the first missing hook), while the driver bridge-write verification fails 5 of 7. Evidence overclaims corrected by the record, none criterion-breaking: the sentence that the new guard checks fail 65 of 68 against the pre-change UI inverts the guard output, which is 65 of 68 passing and 3 failing plus 5 of 7 driver failures; the selection sheet is a role=toolbar outside the shared focus trap and the Escape handler, so the claim that the trap covers it is not true, while the confirmation surfaces and pickers the criterion names are trapped; the per-check negative control for Attach, Remember, origin chip and Return to source collapses into one thrown-check failure rather than individual failures. Constraints hold: ordinary Chat unchanged and never consumed; no transcript inference (bridge host code references no thread name, preview or thread/read); Task is the UI noun and the written kind stays ticket on schema_version 2 with no migration; the only .vibehub commits since 590ecd8 are the orchestrator evidence and the repin Ticket, no Ticket in this repository carries an origin, and no bridge write landed here; no association store, cache or browser storage; every bridge action is gated by requireBoundScope; audio, visual system, packaging and alpha files are untouched.",
  "ticket_id": "ticket-build-codex-chat-task-bridges",
  "unresolved_acceptance_ids": []
}
