{
  "accepted_acceptance_ids": [
    "single-entry",
    "pipeline-documented",
    "survey-before-write",
    "tree-gate",
    "extraction-and-settle",
    "sharding",
    "distill-narrowed"
  ],
  "closed_at": "2026-09-01T00:00:00.000Z",
  "evidence_ids": [
    "evidence-ingest-branches-on-input-shape",
    "evidence-five-pass-reference-written",
    "evidence-survey-precedes-every-write",
    "evidence-tree-is-the-only-human-gate",
    "evidence-segment-citation-and-coverage-gate",
    "evidence-sharding-by-room-not-segment",
    "evidence-distill-narrowed-in-place"
  ],
  "kind": "ticket_outcome",
  "schema_version": 1,
  "status": "successful",
  "summary": "Independent adjudication at 718ee15 (clean tree, npm test 161/161/0, project validate valid:true, npm run verify:artifact ok:true). Every criterion was decided by reading the delivered prose and by running each command the prose cites, resolved from the citing file's own directory. single-entry: git diff 718ee15~1 -- skills/vibehub-ingest/SKILL.md is a single +7/-0 hunk between the capture-only paragraph and '## Workflow'; the frontmatter description and all five numbered workflow steps and the Guardrails section are untouched by the diff, and the added branch keys on shape ('Branch on the shape of what was handed over, never on its size and never by asking the user to choose'), naming no mode, threshold, or Skill for a user to pick. pipeline-documented: skills/vibehub-ingest/references/bulk-absorption.md has one section per pass. Every cited command was resolved from skills/vibehub-ingest/references/ and run: ../../vibehub-core/scripts/vh.mjs and ../../vibehub-core/scripts/vh-ui.mjs both land on the real helpers; source segment returned ok:true (markdown-headings, 9 segments on the reference itself; line-windows L1-60/L61-120 on a .mjs; a PNG is refused, not silently dropped); context put --repo --room --input is the exact signature contextOperation enforces (it throws invalid_input without --room); context coverage --repo . returned ok:true and --room knowledge narrows it. SKILL.md gained only the branch paragraph plus the pointer. Pass 2 carries no command by design because it writes nothing. survey-before-write: Pass 2 is titled 'Survey (writes nothing)' and requires -- not recommends -- reading every segment of every source before anything is written, producing a topic list whose entries carry the segment ids, and states explicitly that nothing is written to .vibehub/ in the pass; Passes 3 and 4 follow it. tree-gate: Pass 3 delegates Room writing to $vibehub-distill and presentation to $vibehub-review, and its invocation 'vh-ui.mjs --repo <root> --rooms' is byte-identical to the one skills/vibehub-review/SKILL.md documents as the Room tree entry rather than invented; run as --rooms --no-open --json it returned ok:true with focus.rooms:true. Step 4 stops for explicit human confirmation before any Context, the section title and body both name it the only human gate, and no other pass stops for a human. extraction-and-settle: Pass 4 requires the exact segment id in source.ref or evidence[].ref and forbids widening to a bare file path; a zero-Context segment requires a stated reason as a {segment, reason} coverage_exceptions entry, which room.schema.json really defines (required segment and reason, additionalProperties false, uniqueness enforced in vh.mjs). Pass 5 states in bold that absorption is not complete while uncovered_total is non-zero and returns the reader to Pass 4 for those exact ids. sharding: the parallel section shards Pass 4 by Room and never by segment with the duplicate-extraction reason stated, assigns cross-Room reconciliation to the orchestrator, declares subagents an optimization and never a requirement with the sequential path performing the identical steps in the identical order, and states that a subagent's completion report is not evidence and that Pass 5 is computed rather than reported. distill-narrowed: the description now opens 'Internal mechanism that writes Room shape and alignment stamps' and closes 'Invoked by $vibehub-ingest, $vibehub-ticket-plan, and $vibehub-migrate, not called directly by a user' -- all three callers verified in the tree (ticket-plan SKILL.md:62, migrate SKILL.md:33, bulk-absorption.md:72). 'A small honest tree beats an exhaustive one' is gone from the Skill and is replaced by splitting pressure naming both required tests: the boundary-conjunction test (a room must split when its boundary needs \"and\" to join two unrelated concerns) and the distinct-reader test (or when its topics serve visibly different readers). Constraints all hold: no new Skill directory; the only name a user must remember is still vibehub-ingest; distill is modified, not deleted; the commit touches no file under .vibehub/rooms/. .vibehub/rooms/knowledge/decision-ambient-two-mode-distill.yaml and decision-stale-origin-layers.yaml were read and judged independently -- the first records cold-start plus align-on-use Room-tree building and a trust layering in which human-ingested Context wins, the second records stale-reason origins; neither claims distill extracts Context nor that a user invokes it by name, and distill wrote only Room shape before this change, so the narrowing is a framing change that falsifies neither. Scope stays out of semantic Room anchors, artifact-authority Context, and Ticket-splitting pressure, and the reference explicitly forbids a second store, manifest, or progress file. On the three disclosed judgment calls: editing skills/vibehub-distill/agents/openai.yaml is required rather than out of scope, since leaving 'Use $vibehub-distill to build...' as a Codex entry prompt would have contradicted the very description the criterion demands; omitting the new reference from scripts/verify-plugin-artifact.mjs breaks nothing -- build-plugin-artifact.mjs ships whole reference directories and the built artifact contains bulk-absorption.md, so the only cost is a missing future regression guard, which no criterion or constraint names; and the knowledge room reporting DRIFTED on this ticket's own edits is correct align-on-use behavior, not a defect, matching decision-ambient-two-mode-distill and the same call recorded in .vibehub/outcomes/ticket-ambient-distill-workflow.yaml. Evidence corrections, none of which change a verdict: evidence-distill-narrowed-in-place says 'git diff --stat reports 17 insertions, 4 deletions' for distill/SKILL.md, but --numstat is 13 insertions and 4 deletions -- 17 is the total changed-line count, misread as insertions; evidence-five-pass-reference-written and evidence-segment-citation-and-coverage-gate both cite context coverage as files_examined 140, segments_total 752, uncovered_total 640, which is now stale -- HEAD reports 141/761/649, the delta being exactly the nine segments of the new reference itself; and evidence-five-pass-reference-written's 'one section per pass, each with its exact command' overstates slightly, since Pass 2 correctly has no command. One imprecision in the delivered prose, not a criterion failure: Pass 1 says a binary file is 'refused and reported as skipped', but source segment refuses with an error envelope and it is context coverage that carries the per-file skipped field.",
  "ticket_id": "ticket-absorb-large-material-through-ingest",
  "unresolved_acceptance_ids": []
}
