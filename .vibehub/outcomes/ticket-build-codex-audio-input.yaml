{
  "accepted_acceptance_ids": [
    "audio-capability-gates-the-microphone",
    "recording-state-timer-and-accessibility",
    "cancel-discards-and-retry-works",
    "permission-denial-is-truthful-and-recoverable",
    "ordinary-audio-turn-submission-proven-live",
    "no-recording-persistence-by-default",
    "realtime-stays-hidden-behind-capability-truth"
  ],
  "closed_at": "2026-08-24T02:34:31Z",
  "evidence_ids": [
    "microphone-gated-and-realtime-hidden",
    "recording-cancel-and-permission-states-proven",
    "ordinary-audio-turn-proven-live-with-locked-mime",
    "no-recording-persistence-and-suites-green"
  ],
  "kind": "ticket_outcome",
  "schema_version": 1,
  "status": "successful",
  "summary": "Independently adjudicated at 46b87c7 by reproduction, not by the executor's summaries. Reproduced on this machine: npm test 318 tests, 316 pass, 0 fail, 2 DSH skips; verify's artifact step green with the 67-file artifact; npm run probe:codex green with the audio and localAudio input checks proven and no failing check; npm run guard:codex green at 124/124 wide in Light and Dark and 125/125 in all four narrow configurations with the motion audit, bridge writes (7/7), lifecycle walk (6/6) and queue walk (12/12) passing. The negative control was re-executed from scratch, one configuration wide/Light: the head guard overlaid on the pre-change shell fails 109/116 with exactly the seven audio areas failing by name and driver exit 1, proving the audio checks are load-bearing inside the passing counts. Code adjudication: the gate is runtime.audioInput === true && getUserMedia present, the disabled control carries the capability contract's own fallback text (capabilities.v1.json via capabilitySnapshot into bootstrap audioInputFallback) as title and aria-describedby, and the DSH audio declaration shows an empty diff across the ticket commits, still unsupported; the recording tray, m:ss clock toward the 90 second cap, pulse, Stop-recording name swap and every announcement travel through the aria-live polite composer note; all nine exit paths were independently enumerated and each funnels through the controller's single settle() that stops MediaStream tracks (stop, cancel, Escape at app.js:4034, cap timer, size bound in ondataavailable, denial with no acquired stream, awaited finishForSend before the input array is built, audio-chip removal at app.js:3721, and teardown via ensureReleased on route change, capability loss and new Thread); the static no-storage-and-no-object-URL regex covers app.js plus composer-recording.mjs in two node tests; submission routes attachments through validateInputs and sendThroughHarness into harness.sendChatAudio for one turn/start on both the startTurn and queue-head paths with the guard asserting exactly one turn/start and the sent message rendering its audio chip. The live seam was not re-spent: the lock's audio block records acceptedDataUrlMimeTypes audio/webm;codecs=opus with provenance naming one ephemeral read-only Turn on the authenticated 0.149.0 reaching turn/completed status completed and the restarted listing omitting the ephemeral Thread; probe-live.mjs imports RECORDING_MIME_TYPE from the shell module itself, asserts turn/completed for the audio turn id, throws if the ephemeral Thread is ever listed, and stops both clients in a finally; the reported earlier pre-Turn failure is structurally corroborated because the removed stable-thread guard threw before any thread/start, though no captured log of that launch exists. realtimeConversation stays hardcoded false, the guard asserts the absence of realtime, transcript or listening controls, the lock's probe result stays protocol-present-current-ephemeral-thread-unsupported, and the extended exposes-ordinary-audio-honestly test pins the mime, provenance and no-transcription wording. All seven constraints hold, reading the one-Turn probe constraint as this ticket's addition of exactly one audio Turn (the pre-existing probe also runs its sentinel and immediately interrupted handoff Turns per invocation). Minor evidence overclaims that do not defeat any criterion: the storage regex lives in two node source tests, not a host test plus a node test; the realtime word ban lives in codex-first-shell.test.mjs, not the conformance test; event-feed elision is enforced on host-owned queue projections while raw runtime notifications append unfiltered to the in-memory ring; and the negative control's every-configuration claim was re-verified at one configuration with an identical per-page mechanism.",
  "ticket_id": "ticket-build-codex-audio-input",
  "unresolved_acceptance_ids": []
}
