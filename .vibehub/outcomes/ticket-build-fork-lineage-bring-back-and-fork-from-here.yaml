{
  "accepted_acceptance_ids": [
    "source-chip-and-fork-listing-are-production",
    "bring-back-is-an-explicit-labeled-send",
    "fork-from-here-uses-the-stable-seam",
    "fork-surfaces-hold-the-baseline-and-scope"
  ],
  "closed_at": "2026-08-24T00:00:00Z",
  "contract_binding": {
    "binding": "native",
    "digest": "99b7b66ef9bcc8f69e6bfba85291be8e94cf4b847a65c3d0e5878fb87e651840"
  },
  "evidence_ids": [
    "source-chip-listing-and-missing-state-production",
    "bring-back-explicit-labeled-and-replayed",
    "fork-from-here-on-last-turn-id-proven-live",
    "fork-baseline-scope-and-gates"
  ],
  "kind": "ticket_outcome",
  "schema_version": 2,
  "status": "successful",
  "summary": "Independent adjudication at 3f1e0a6 accepts all four criteria; every Evidence claim was re-derived from code and reproduced by exit code, not read from the executor's summary. All four native acceptance_bindings were recomputed with the exported contractIdentity and matched the recorded per-criterion digests exactly. Gates reproduced independently: npm test exit 0 at 354 tests / 352 pass / 0 fail / 2 skips, npm run verify exit 0, npm run probe:codex exit 0, npm run guard:codex exit 0 at 138/138 wide twice and 139/139 narrow four times with zero failing checks and motion, bridge, lifecycle and queue walks green. All six guard frames reported writes true with zero bridge-skips, so the seeded real-host fork-from-here group ran in every frame rather than taking its no-driver-owned-shell fallback; the driver prints only failures, which is why individual check names are absent from a passing log. Criterion 1: the raw ' Fork of ${...}' heading line present at cd05550 app.js is deleted, with only a fixtures preview string, guard assertions and a static test asserting its absence remaining; the divergence note in fork-lineage.mjs is a true common Turn-id prefix over both read transcripts, returning null when the source transcript is unreadable, so nothing is invented from names or counts. The recorded staleness caveat is narrower than it reads: openThread invalidates the opened fork's cached note, so it is genuinely re-derived on each open, and the only residual drift is the source-total denominator while the reader stays on an open fork; the shared-prefix count itself cannot go stale because appending source Turns cannot change a common prefix. 'Never invented' holds. Criterion 2: the guard asserts onlyReadsDispatched over readThread and listQueue alone plus nothingSent over startTurn, queueTurn and steerTurn, then exactlyOneStart on the explicit human send and the replay rendering; focusStaysOnThreadTitle matches the corrected contract, which itself documents focus remaining on the thread title, so the Evidence's 'no deviation exists' is accurate rather than a waiver. Criterion 3: the fixture app-server implements lastTurnId as an inclusive boundary via terminal.slice(0, indexOf(named) + 1), refuses an in-progress boundary and rejects an unknown turn, matching the pinned upstream claims in fork-interaction-contract.json; a static test proves thread/rollback appears nowhere in shell or adapter. The live proof was not re-run: its retained script spends exactly two turn() calls and no more, thread/fork spends none, and the recorded JSON is internally consistent. The deletion claim was confirmed independently and read-only at zero Turns against the same installed 0.149.0 runtime string: thread/list over the disposable cwd returns zero active and zero archived, and both recorded thread ids read back as 'thread not loaded'. Criterion 4: the conformance matrix moved 31 to 34 checks adding exactly fork-source-chip-and-listing, fork-bring-back-to-source and fork-from-here with none removed and no downgrades, and the enforcing test resolves every path::name proof entry to a really declared test or guard check while pinning the tally at pass 32, partial 0, deferred 2, fail 0 against 29 pass at cd05550. Constraints hold: Compare appears nowhere in the production shell; the sidebar tree stays behind state.forkReview.direction !== 'sidebar' with forkReview null in production, so Direction B is held; no fork-point field is persisted anywhere; no summarization or transcription exists in the bring-back path; the two-Turn live budget and the deletion requirement are both met; and the Evidence claims contrast floors only, never final visual identity. Three honest notes that change no verdict. The negative control's method is coherent in code and its failures would concentrate in the new work, but only three catch-wrapped group failures are identifiable statically against the four claimed, and the control was not re-run here, so its exact 124-of-128 split stands unreproduced and is treated as supporting rather than load-bearing. bringBackQuote caps the carried passage at 4000 characters, which is verbatim truncation rather than summarization and so violates no constraint, but the cap is undisclosed in the Evidence. The Evidence itself discloses that the implementer mistakenly terminated a demo shell's app-server child in a different worktree during cleanup, which self-recovered; recording it was correct and it affects no criterion.",
  "ticket_id": "ticket-build-fork-lineage-bring-back-and-fork-from-here",
  "unresolved_acceptance_ids": []
}
