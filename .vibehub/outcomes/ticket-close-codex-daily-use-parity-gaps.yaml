{
  "accepted_acceptance_ids": [
    "follow-ups-queue-by-default-and-steer-is-explicit",
    "model-and-effort-pickers-are-truthful",
    "images-paste-drop-and-multi-attach",
    "file-and-skill-mentions-from-the-composer",
    "context-usage-and-compaction-are-visible",
    "rename-and-settings-posture-per-thread",
    "turn-completion-notifies-once-and-sidebar-stays-fresh",
    "parity-surfaces-hold-the-baseline"
  ],
  "closed_at": "2026-08-23T10:11:58Z",
  "evidence_ids": [
    "follow-up-queue-default-with-explicit-steer",
    "model-and-effort-pickers-from-model-list-only",
    "image-paste-drop-and-multi-attach",
    "file-and-skill-mentions-with-exact-byte-ranges",
    "context-indicator-and-compaction-from-protocol",
    "inline-rename-and-permissions-posture",
    "completion-notice-once-and-sidebar-freshness",
    "sidebar-freshness-keyed-on-durable-user-message",
    "parity-surfaces-baseline-lock-pins-and-real-runtime-walk"
  ],
  "kind": "ticket_outcome",
  "schema_version": 1,
  "status": "successful",
  "summary": "Second independent closeout at commit 59a873e (clean worktree, codex-cli 0.149.0 installed), replacing the partial Outcome recorded at 5198fe9 after the fix cycle 1a371e4, 219c6f9 and 68f6d05. Every criterion was adjudicated again from reproduced proof, not from the executor narratives. Reproduced: npm test 295 tests, 293 pass, 0 fail, 2 DSH-environment skips; npm run verify passes (62 artifact files); npm run probe:codex ok with 65 of 65 checks proven on the installed binary, protocol hash 9b3de71a; npm run guard:codex on the fixture passes 109 of 109 wide in Light and Dark, 110 of 110 narrow-window and narrow-viewport in Light and Dark with zero console errors and no horizontal overflow, bridge writes 7 of 7 in all six frames, lifecycle walk 6 of 6 with the brand-new Chat's own dot true and two thread/list calls issued between its turn/started and the durable userMessage, follow-up queue walk 12 of 12 (call log start:first, completed, start:follow-up one, completed:interrupted, start:follow-up two, completed, start:fourth, completed across four distinct Turn ids; text_elements byte ranges 5-15 and 21-36; Context 3% 8,700 of 272,000 to 0% 400 of 272,000 after one thread/compact/start; thread/name/set then thread/name/updated; never plus dangerFullAccess then on-request plus workspaceWrite on successive turn/start; one background completion notice with the DONE badge). The real-runtime lifecycle walk (--runtime real on the disposable bound repository) passed 6 of 6 with the own-dot clause now inside the step's pass condition, and both Threads it created were deleted. The 28 criterion-specific node tests were rerun by name and pass; git diff 137c040..59a873e on the test files shows the fix cycle only added tests and re-pinned notification order (thread/status/changed before turn/started, the userMessage item before the model's first request, thread/read in the startTask call log) without weakening any assertion behind the seven previously accepted criteria, so they are re-accepted on the same reproduced proofs. For turn-completion-notifies-once-and-sidebar-stays-fresh: the once-per-Turn notice (node test delivers the same turn/completed live then replayed with one notice and one stubbed Notification construction; preference set through setNotificationPreference, back to unfocused after a host restart; no localStorage, sessionStorage, IndexedDB or cookie write in source by static test and my own grep) still holds; the sidebar clause is now met by apps/codex-first-shell/sidebar-freshness.mjs, read in full: the refresh cue is the watched Thread's userMessage item/completed (or its turn/completed while still unlisted), thread/status/changed only moves row status, the retry is bounded to LISTING_RETRY_ATTEMPTS 4 refreshes LISTING_RETRY_DELAY_MS 750 apart each scheduled after the previous settled, nextRetry returns null once the budget is spent, the watch ends the moment a bootstrap lists the Thread, a runtime exit clears every watch, app.js has no setInterval, and the record the browser already holds is drawn as a provisional live row at turn/started. The fixture now omits a brand-new Thread from thread/list until its userMessage item is completed by default (CODEX_FIXTURE_LIST_NEW_THREADS durable, immediate restores the old listing), proven by the host test that walks absent at thread/start, absent at turn/started, listed active after item/completed, listed idle at turn/completed. Five in-page guard checks were read with their detail lines on a fixture shell: turn/started draws the held record live with refreshes unchanged and a retry pending, the first retry's bootstrap misses the Thread and the row survives, the userMessage item/completed refreshes at once, the listing bootstrap replaces the row with the host's record (title guard listing walk) and the refresh count is stable afterwards with zero pending timers, and thread/status/changed idle settles the dot without a refresh. Negative control: HEAD with only app.js rolled back to 137c040 fails the guard frame 103 of 105 (the freshness checks throw on the missing listingWatch hook and the Task-Thread quote check fails for the same unlisted-window reason) while bridge, lifecycle and queue walks still pass, so the new checks discriminate the fix; the lifecycle walk's own-dot step is not discriminating on its own because its 15 s wait exceeds the browser's 10.2 s periodic refresh, which the Evidence does not state. To close that gap a real-runtime measurement was taken with one short model Turn in a brand-new Chat: turn/started reached the host 123 ms after submit, the userMessage item/completed at 1,068 ms, thread/status/changed idle and turn/completed at 2,737 ms, the host's thread/list first carried the Thread at about 3,040 ms (status active), while the Sidebar's own live dot appeared at 938 ms, 815 ms after turn/started and before the host ever listed the Thread, and settled idle at 4,787 ms; the probe Thread was deleted. Constraints hold: no thread/queue/*, collaborationMode or permissionProfile in host, UI or adapter (the lock names thread/queue/changed only in the 0.149.0 baseline diff and as unpinned); queue, settings and preference are host memory only with the restart proof; Queue is the Send default while a Turn streams in ordinary Chat with Alt+Enter and per-row Steer explicit; no photos library, appshot, pasted-text-as-file, terminal or Finder, auto titling or plan mode; the host's only git call is rev-parse --show-toplevel; the only .vibehub change since 137c040 is evidence commit 59a873e; nothing was committed by this closeout. Narrative caveats in the new Evidence: its single-probe timings (userMessage durable about 430 ms, first listing about 1.2 s after turn/started) were slower in my run (about 945 ms and 2.9 s), still inside the recorded 1.5 to 3 s constraint and the retry budget; and the real walk's own-dot assertion should be described as a bounded-wait presence check, not a timing proof. Follow-up gaps, none blocking: the real thread/list still reported the Thread active when it first listed it about 300 ms after turn/completed, so a bootstrap landing there keeps the live dot until a later poll settles it (observed settled about 2 s after turn/completed); the provisional row of an unnamed new Chat is titled Untitled task until the host's record carries the preview; the previously noted Evidence wording overclaims in the queue and baseline records remain as written.",
  "ticket_id": "ticket-close-codex-daily-use-parity-gaps",
  "unresolved_acceptance_ids": []
}
