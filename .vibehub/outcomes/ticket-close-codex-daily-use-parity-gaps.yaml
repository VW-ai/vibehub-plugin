{
  "accepted_acceptance_ids": [
    "follow-ups-queue-by-default-and-steer-is-explicit",
    "model-and-effort-pickers-are-truthful",
    "images-paste-drop-and-multi-attach",
    "file-and-skill-mentions-from-the-composer",
    "context-usage-and-compaction-are-visible",
    "rename-and-settings-posture-per-thread",
    "parity-surfaces-hold-the-baseline"
  ],
  "closed_at": "2026-08-23T08:56:44Z",
  "evidence_ids": [
    "follow-up-queue-default-with-explicit-steer",
    "model-and-effort-pickers-from-model-list-only",
    "image-paste-drop-and-multi-attach",
    "file-and-skill-mentions-with-exact-byte-ranges",
    "context-indicator-and-compaction-from-protocol",
    "inline-rename-and-permissions-posture",
    "completion-notice-once-and-sidebar-freshness",
    "parity-surfaces-baseline-lock-pins-and-real-runtime-walk"
  ],
  "kind": "ticket_outcome",
  "schema_version": 1,
  "status": "partial",
  "summary": "Independent closeout at commit 5198fe9 (clean worktree) on the installed codex-cli 0.149.0, reproducing every proof rather than accepting the executor summaries. Reproduced: npm test 292 tests, 290 pass, 0 fail, 2 DSH-environment skips; npm run verify passes (62 artifact files); npm run probe:codex ok with 65 of 65 checks proven on the installed binary and the lock hash 9b3de71a; npm run guard:codex on the fixture passes 105 of 105 wide in Light and Dark, 106 of 106 narrow-window and narrow-viewport in Light and Dark with zero console errors and no horizontal overflow, bridge writes 7 of 7 in all six frames, lifecycle walk 6 of 6 and the follow-up queue walk 12 of 12 (fixture call log start:first, completed, start:follow-up one, completed:interrupted, start:follow-up two, completed, start:fourth, completed across four distinct Turn ids; exact text_elements byte ranges 5-15 and 21-36; Context 3% 8,700 of 272,000 to 0% 400 of 272,000 after one thread/compact/start; thread/name/set then thread/name/updated; approvalPolicy never plus sandboxPolicy dangerFullAccess then on-request plus workspaceWrite on successive turn/start; one background completion notice with the DONE badge). The real-runtime lifecycle walk was rerun with --runtime real on a disposable bound repository and passed 6 of 6; a host-level probe on the real runtime read 7 models from model/list with displayName, isDefault, defaultReasoningEffort, supportedReasoningEfforts and inputModalities, a thread/start settings record (gpt-5.6-sol, effort xhigh, on-request, workspaceWrite, source thread/start), one thread/tokenUsage/updated inside the Turn before turn/completed (20,795 of 258,400), thread/name/set followed by thread/name/updated reflected in the bootstrap row, fuzzyFileSearch rooted at the bound repository and skills/list with that cwd; every Thread created (four) was deleted afterwards. Accepted on reproduced proof: follow-ups-queue-by-default-and-steer-is-explicit (host test timeline, distinct ids, paused until Resume, exact expectedTurnId, queue lost on host restart; six guard checks; matrix row rewritten with resolving proofs); model-and-effort-pickers-are-truthful (own grep finds no model or effort option string outside the guard's fixture data; pickers disabled Not loaded until listModels; model slug and effort as turn/start keys; text-only refusal names displayName and inputModalities; matrix row model-mode-pickers after deferred to pass, 29 pass and 2 deferred; conformance assertion narrowed to mode and realtime); images-paste-drop-and-multi-attach (host validateInput accepts only data: URLs for image and audio within the body bound and refuses localImage; node test and guard cover paste, drop, multi-attach and removal); file-and-skill-mentions-from-the-composer (host validateTextElements compares each byteRange with Buffer.byteLength and the bytes at the span; node tests and the queue walk carry the exact input arrays); context-usage-and-compaction-are-visible (contextUsage unknown before the first notification, no percentage on a null or zero window, 409 turn_live, contextCompaction boundary, no thread/compacted on the v2 path); rename-and-settings-posture-per-thread (exact posture pairs, alertdialog with Tab trap, Escape and focus restore, Turn label; per-Turn posture is visible only for Turns this browser session started because thread/read carries no per-Turn settings); parity-surfaces-hold-the-baseline (lock pins and probe, contrast pairs, keyboard audit, proof fields resolve, negative control reproduced from the scratchpad overlay). Unresolved: turn-completion-notifies-once-and-sidebar-stays-fresh. The once-per-Turn notice is proven (node test delivers the same turn/completed live then replayed with one notice and one stubbed Notification; preference transient; no browser storage), but the clause that the sidebar refresh on turn/started makes a brand-new Chat's first Turn show its live dot on the real runtime is contradicted by three independent real-runtime observations: the executor's own walk log records own dot false, my rerun of the lifecycle walk records own dot false, and two host-level probes show that at turn/started the refreshed thread/list does not contain the new Thread (immediate refresh and an 850 ms browser-cadence refresh both unlisted; the Thread is listed only once its userMessage is durable, about 1.5 to 3 seconds later, and at turn/completed it is listed idle). The mechanism fires on the fixture, which lists Threads immediately, so the guard cannot detect this; browser-runtime-review.md already recorded that the real app-server lists a Thread only after the userMessage item completes, so turn/started is the wrong cue. Overclaims in the Evidence narrative: the queue Evidence calls the fixture-shell walk a real-host walk while the driver skips the queue walk on the real runtime, and says three queued messages became three turn/start requests when the host test starts two of three (one deleted) and the walk starts two of two; the baseline Evidence says the negative control overlaid the new guard on the pre-change host and UI, but the scratchpad overlay is app.js from 2b7a20d with the host from 801c536, so it is a browser-side negative control only; the notification Evidence states the sidebar refresh shows the live dot without a real-runtime observation. Constraints hold: every option, value and posture comes from model/list, the thread/start, thread/resume and thread/settings/updated records or the pinned schema enums; no thread/queue/*, collaborationMode or permissionProfile in host, UI or adapter (experimentalApi true predates this Ticket for audio); queue, settings and preference are host memory only with static tests against browser storage and a host restart proof; Queue is the default while a Turn streams in ordinary Chat with Alt+Enter and per-row Steer explicit (the Task Workspace composer keeps steer as its labelled default, a pre-existing surface outside this Ticket's ordinary Chat scope); no photos library, appshot, pasted-text-as-file, terminal or Finder, auto titling or plan mode; no visual identity or audio claim; the host's only git call is rev-parse --show-toplevel; the only .vibehub change since 2b7a20d is evidence commit 5198fe9. Follow-up gaps: refresh the sidebar on the real runtime from a cue that follows the durable userMessage (item/completed for the userMessage, thread/status/changed, or a short retry after turn/started) and assert the own dot in the real lifecycle walk; correct the three Evidence wordings above; compactThread on an unknown Thread answers 500 internal_error rather than a typed refusal; fuzzyFileSearch on the real runtime returns .git internals, so the @ picker offers them.",
  "ticket_id": "ticket-close-codex-daily-use-parity-gaps",
  "unresolved_acceptance_ids": [
    "turn-completion-notifies-once-and-sidebar-stays-fresh"
  ]
}
