{
  "accepted_acceptance_ids": [
    "in-app-switching-entry",
    "previous-session-fully-released",
    "switch-confirmation-is-consistent",
    "recents-reflect-real-use",
    "switching-changes-no-ticket-state"
  ],
  "closed_at": "2026-08-11T00:41:34Z",
  "evidence_ids": [
    "evidence-switch-in-app-switching-entry",
    "evidence-switch-previous-session-fully-released",
    "evidence-switch-confirmation-is-consistent",
    "evidence-switch-recents-reflect-real-use",
    "evidence-switch-changes-no-ticket-state"
  ],
  "kind": "ticket_outcome",
  "schema_version": 1,
  "status": "successful",
  "summary": "Independently adjudicated at commit 84576fd231937ad1704dd13b4f83d5b51d25dc9f (Evidence 47ea1fc) by rebuilding the binary, building the PRE-CHANGE binary for comparison, and re-running every proof against my own fixtures rather than reading the executor's summary. All five criteria accepted. IN-APP ENTRY: --probe-menu over the real binary prints a File menu holding 'Open Repository…' with key 'o', modifiers [command] and action openRepository:, plus a Recent Repositories submenu whose items carry the basename as title and the exact absolute path as representedObject and tooltip bound to openRecentRepository:, and a single disabled 'No Repositories Opened Yet' item when nothing is remembered. main.swift installs the same WorkbenchMenu type the probe installs, so the probe is not a parallel description. --probe-switch constructs the real AppDelegate and sends the chosen item's action with NSApplication.sendAction(action, to: item.target=nil, from: item), so it travels the responder chain exactly as a menu-bar click does: actionSent true, one process, one delegate, no relaunch. WINDOW REPLACED, FRAME GENUINELY REUSED: windowRecreated true (the second NSWindow has a different ObjectIdentifier), and the compiled open() slice contains no webView reference, so the live WKWebView is never re-pointed - the crash WorkbenchWindowController documents. I reproduced the executor's exact claim (seeded frame {{140, 140}, {1000, 700}} -> first window {{140, 140}, {1000, 700}} -> second window {{140, 140}, {1000, 700}}, windowFrameReused true) and then ran a STRONGER case with my own frame {{222, 333}, {900, 640}}: AppKit constrained it to {{222, 304}, {900, 640}} for the first window, and the window built after the switch came up at exactly {{222, 304}, {900, 640}} - identical to the frame the session actually had. The frame the user arranged therefore carries across window replacement. Noting honestly that the probe's windowFrameReused flag compares the new frame against the SEED string rather than against the first window, so it reads false whenever AppKit constrains the seed; that is a flag-definition artifact, not a behaviour defect, and the first==second comparison I ran is the sounder measurement. PREVIOUS SESSION RELEASED: in my own accepted switch, previousHostAlive false (kill(pid,0) fails), previousOriginStatus -1 on the old /health, previousAuthorizedUrlStatus -1 on the exact authorized URL the WKWebView had been loading, and previousAuthorizedApiStatus -1 on the old /api/state carrying the bearer that was valid moments earlier; the new host is on a different port (55765 -> 55774). The refused run is the positive control that validates those -1 readings: through the identical code path it returns 200 and 200, so -1 means nothing answered rather than a broken probe. No preference key holds a token: preferenceKeys were exactly [recentRepositories, windowFrame], a subset of PreferenceKey.allowlist; bearerInPreferences false from a 64-hex scan of every stored value; and I independently confirmed the whole printed envelope contains no 64-hex sequence. firstHostAliveAtExit and secondHostAliveAtExit both false, and externally no VibeHubWorkbench process or listening port survived any of my runs. TEARDOWN REORDER JUDGED SOUND: HostSession.start for the new repository now precedes host?.terminate() for the old, which precedes workbench?.close(), the new controller and show(). A failed start throws before anything is committed, so host, currentRepository and workbench are untouched and the running session survives - the intended property. I checked the converse risk too: nothing after 'let started = try HostSession.start' can throw (WorkbenchWindowController.init does not throw), so a started host cannot leak. Two hosts do overlap between start and terminate, but only for a few synchronous statements with no run-loop turn, only ever on DIFFERENT repositories (the same-repository case returns focusCurrentSession and never reaches open() while a window exists), both read-only on separate loopback ports, and with no window on screen for the new one. Termination still precedes the new session becoming visible, which is what the criterion requires. CONFIRMATION CONSISTENT, DEEP LINK NOT REGRESSED: requestRepository is the single in-app entry for the Cmd-O panel, the recents submenu and the launch picker, and it calls the same DeepLinkPlanner.resolve with the same currentRepository and the same remembered list vibehub://open uses. The real AppKit sheet was read back out of the view hierarchy: title 'Switch the Workbench to a different repository?', buttons 'Switch Repository' and 'Stay Here', detail naming both worktrees and the consequence sentence, promptMatchesSheet true. Refusal leaves everything: switched false, currentRepository unchanged, second null (the other host was never started), previousHostAlive true with origin 200 and authorized API 200, windowRecreated false, recentsAfter identical to recentsBefore. I verified the byte-identity claim rather than trusting it - I built the binary at 84576fd~1 in a throwaway worktree and ran --probe-deep-link with identical arguments against both binaries: the confirm-switch prompt is byte-identical, sha256 4cf3dde1678f9ed25aece8e256d0276e085deb7f826eda445e155d94429b0824 on old and new alike. The closed deep-link Ticket is not silently regressed, and its own suite passes untouched. The one deliberate difference is the requester line - .userSelection says 'You asked to open:' where .deepLink says 'A vibehub:// link asked for:'. The criterion asks for the same confirmation SEMANTICS, not identical bytes, and semantics are identical (same planner, same obligation to answer, same title, buttons and consequence, refusal changes nothing); the differing line exists so the sheet does not tell a user who picked a worktree from a menu that a link asked for it. RECENTS PRUNE BY ERROR CLASS: I exercised all four classes over the real binary against real on-disk directories - a deleted path -> notADirectory, permanent, dropped; <repo>/.vibehub -> notAnExactWorktree, permanent, dropped (the defect independent validation flagged, genuinely fixed); a git repository with no .vibehub -> notAVibeHubRepository, permanent, dropped; and gitFailed -> permanent false, dropped [], entry retained, for BOTH a plain directory and a .vibehub-without-git directory. The transient case is genuinely reachable and simulable, not an asserted code path in principle: it comes from a real git rev-parse subprocess exiting non-zero, carrying git's own text 'fatal: not a git repository (or any of the parent directories): .git'. The live --probe-switch to such a path likewise kept the entry (recentsAfter == recentsBefore), stated the failure on the open window, and left the running session alive at 200. Every case carries a non-empty human error string and never takes an unrelated entry with it; a successful open puts that worktree first. Honest nuance recorded: the mechanism used to reach gitFailed - 'not a git repository' - is arguably a durable property rather than a momentary one, so a path that will never validate can persist in recents; the criterion names gitFailed explicitly as the class to keep and the dispatch is on error class, so this is correct as specified rather than a defect. NO TICKET STATE CARRIED OR WRITTEN: with my own fixtures (2 tickets vs 4, deliberately different from the executor's 1 vs 3) the projection before the switch reported worktreeRoot alpha, branch alpha-br, 2 Tickets [ticket-aa, ticket-ab] and after reported worktreeRoot beta, branch beta-br, 4 Tickets [ticket-ba, ticket-bb, ticket-bc, ticket-bd], each fetched from its own host with that host's own bearer - a carried-over projection would have shown alpha's root and 2. I hashed both fixtures' entire .vibehub trees myself (per-file sha256, path-sorted, rolled into one digest) immediately before and after each probe: unchanged in both worktrees across the accepted switch (alpha c26b5484…, beta 1eb56127…), unchanged in both across the refused switch, and unchanged in alpha across a switch to an invalid path and to a directory git cannot read. CONSTRAINTS: the implementation commit touches no .vibehub YAML and nothing under skills/, so vh-workbench.mjs and the shared frontend read-only contract are untouched and repository selection stays shell-owned; RepositoryOpen.attempt is the single validation and pruning point shared by the picker, both menu entries, the deep link and the probes, and AppDelegate contains no NSOpenPanel of its own (one panel, owned by the picker, sheeted on whichever window the user is reading); no FSEvents, NSMetadataQuery, scheduled timer, launchd or directory-enumeration primitive exists in the shell sources, so nothing daemonizes or scans for repositories. SUITES: npm run verify reports 87 tests, 87 pass, fail 0, skipped 0, with the artifact verifier ok, and I rebuilt the artifact independently and counted 52 files with 0 apps/ entries. test/workbench-switch-repository.test.mjs is 10/10 with 0 skipped, so the window-server-dependent switch probes genuinely ran rather than skipping. test/ui-host.test.mjs, test/session-watch.test.mjs, test/workbench-launcher.test.mjs, test/workbench-deep-link.test.mjs and test/workbench-shell.test.mjs are byte-identical (sha256 compared) between 84576fd~1 and HEAD, so no security or invariance assertion was weakened - they were not touched at all. The deep-link Ticket's regression guards are intact and still asserted: runModal() appears nowhere in any Swift source, so no application-modal alert can be raised at launch, and isReleasedWhenClosed = false is present in both WorkbenchWindowController and RepositoryPickerWindowController; the launch and first-open flow still works, exercised end to end at the head of every switch probe, and the picker now routes through requestRepository where a first open resolves to openKnownRepository or confirmFirstUse and opens without a redundant second question. HEADLESS-PROOF SUFFICIENCY, PER CRITERION: GUI automation is denied here, so no menu item was clicked, no live sheet answered and no directory chosen in NSOpenPanel. in-app-switching-entry - sufficient for the recents entry, whose action was sent through the real responder chain and produced a real switch; partial for Cmd-O, where only the binding and its responder resolution (NSApplication.target(forAction:to:from:) returning AppDelegate) are proven and no directory was chosen, which the criterion's literal wording tolerates because it requires driving the open path a selection invokes and both entries provably funnel into the one requestRepository. previous-session-fully-released - fully sufficient; pid death, port reachability and preference contents are exactly what a headless probe measures best and a click would add nothing. switch-confirmation-is-consistent - sufficient; the sheet was really presented by the real app on the real window and read back from AppKit's own view hierarchy, then ended with the exact return code each button produces, leaving unproven only AppKit's internal mapping from a click to that return code, not any application logic. recents-reflect-real-use - fully sufficient; error classification and preference-list state are wholly observable headlessly, and the failure sheet was additionally read back from the real window. switching-changes-no-ticket-state - fully sufficient and in fact stronger than manual observation, since byte-level tree hashing and per-host projection reads are more precise than anything a human clicking could establish. PROCESS HYGIENE: the two running vh-workbench.mjs sessions (pids 96028 and 52845) belong to the repository owner and were left untouched; every host I started exited with its probe; the throwaway worktree built for the old-binary comparison was removed; and both real preference plists are byte-identical (sha256) to their pre-adjudication state with no new preference domain created, confirming the probes' in-memory UserDefaults.",
  "ticket_id": "ticket-workbench-switch-repository",
  "unresolved_acceptance_ids": []
}
