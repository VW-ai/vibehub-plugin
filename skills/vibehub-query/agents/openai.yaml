interface:
  display_name: "VibeHub Query"
  short_description: "Retrieve and synthesize governed project context"
  default_prompt: "Use $vibehub-query to retrieve the project context needed for this task."
