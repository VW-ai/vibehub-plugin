interface:
  display_name: "VibeHub Query"
  short_description: "Retrieve governed project context at useful depth"
  default_prompt: "Use $vibehub-query to assemble the governed context needed for this task."
