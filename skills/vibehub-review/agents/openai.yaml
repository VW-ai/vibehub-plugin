interface:
  display_name: "VibeHub Review"
  short_description: "Resolve quality and lifecycle findings"
  default_prompt: "Use $vibehub-review to inspect and resolve the current knowledge review queue."
