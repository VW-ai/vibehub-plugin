interface:
  display_name: "VibeHub Ingest"
  short_description: "Explicitly capture durable project context"
  default_prompt: "Use $vibehub-ingest to record this durable context in Git-native VibeHub YAML."
