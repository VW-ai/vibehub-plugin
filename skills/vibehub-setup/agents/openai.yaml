interface:
  display_name: "VibeHub Setup"
  short_description: "Set up lightweight Git-native VibeHub"
  default_prompt: "Use $vibehub-setup to detect existing memory systems and set up lightweight VibeHub in this checkout."
