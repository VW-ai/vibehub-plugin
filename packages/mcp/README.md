# @vw-ai/workbench-mcp

The local-first VibeHub MCP server.

Run it without a global install:

```bash
npx --package @vw-ai/workbench-mcp vibehub-mcp
```

Source and documentation: <https://github.com/VW-ai/vibehub-plugin>
